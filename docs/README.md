# docs/ index

Research findings, design decisions, and experiment write-ups for the VGGT-MRI project. Numbered
`NN_*.md`, each opening with a `> **TL;DR & takeaway**` blockquote (see `CLAUDE.md → Docs` for the
convention). Separate from per-version implementation logs in `version_history/`.

**This index is the entry point** — skim it to find the right doc, then open only that one. Add a
one-line pointer here whenever you create a new `docs/NN_*.md`.

## ⭐ Standing instructions (unnumbered — read these, they are not findings)

- **[`experiment_evaluation_instruction.md`](experiment_evaluation_instruction.md) — HOW TO
  EVALUATE AND COMPARE RUNS. Read this before reporting any number from a run.** Where the metrics
  live on disk (`val_per_subject.csv` / `metrics.jsonl` / `run_meta.jsonl` /
  `baseline_identity.json` — **not** wandb), what every `metric_*` means, which to lead with
  (`psnr_3d_motion` + `recov_frac_heart`), which is a veto not a score (`hole_frac_heart`), which
  to ignore (`psnr_3d_full`/`bbox`), the docs/38 ship rule, how to compare two runs (paired
  per-subject + the commensurability check, over a plateau not one epoch), the hard
  incomparability boundaries (canonical-grid, native-z, slice-order, clamp removal), and the known
  logging gaps.

## Dataset provenance READMEs (separate from these docs)

Each dataset directory carries its **own** `README.md` written for future agents — what the data is,
how it was obtained/reconstructed, verified-vs-inferred facts, and gotchas. These live on gitignored
GPFS next to the data, **not** in `docs/`. When you touch a dataset, read (and update) its README:

- `scratch/data/CMRxRecon2023/README.md` (= `CMRxRecon-300/`) — 2023 paper raw k-space (hand-curated)
- `scratch/data/CMRxRecon2024/README.md` — 2024 challenge k-space; the current training source
- `scratch/data/CMRxRecon2025/README.md` — 2025 multi-center/disease; **TrainingData downloaded 2026-07-23** (not yet reconstructed)
- `scratch/data/{ocmr,goettingen,mnms,fiss,FRF}/README.md` — OOD / baseline eval sources

When you add a new dataset, write a `README.md` in its directory following this same convention.

| Doc | Summary |
|---|---|
| [`01_respiratory_motion_simulation.md`](01_respiratory_motion_simulation.md) | Respiratory motion simulation: literature survey (motion models, magnitudes, phantoms, SVR code), current-data format recap, and design plan. **Simulation core implemented** (2026-06-11): `training/data/respiratory.py` + `tests/test_respiratory.py` + example report `_html/06_respiratory_motion_simulation_examples.html`; GPU-aug/config/trainer wiring deferred. |
| [`02_related_work_literature_review.md`](02_related_work_literature_review.md) | Ranked related-work sweep for the project goal (single/few-frame-per-slice → volume at a target phase): INR SVR (NeSVoR/SVoRT), phase-conditioned template-deformation (5D-MoCo/M-DIP/STINR), real-time cine + gated→real-time gap, classical 5D MR baselines, and the VGGT/DUSt3R geometry lineage. |
| [`03_cardiac_svr_literature_review.md`](03_cardiac_svr_literature_review.md) | Focused classical/DL cardiac SVR sweep (52% adversarial kill rate). Confirms the open gap. Closest classical: Jantsch 2013 (free-breathing 2D cine stacks → 3D+t, many frames/slice). Closest DL: Chen 2024 (motion-correction + through-plane SR → 3D volume, breath-hold). Confirms XD-GRASP, TetHeart, DMCVR are NOT cardiac SVR baselines. |
| [`04_inference_information_contract.md`](04_inference_information_contract.md) | **Design stance** (not yet implemented): at inference assume the model knows only `z` per input slice — input cardiac `t` and respiratory `r` are UNAVAILABLE (one-frame-per-slice ⇒ no self-gating; no ECG/respiratory device assumed). Target queries (`target_t`/`target_r`) stay free (chosen, sim GT). Pin `target_r` (4D, correct-not-resolve) since input `r` is unseeable; ECG/self-gating are the fallback via input-phase dropout. |
| [`05_respiratory_variants_results.md`](05_respiratory_variants_results.md) | **5-run results** (2026-06-16): cross-task re-eval of respiration × input-`t` × aggressive-aug. Breathing simulation is decisive — no-resp models sit at the do-nothing identity floor on breathing val; the resp model beats its no-resp twin by **+3.33 dB bbox / +2.24 dB motion**, ~free on the clean task. Input-`t` is unnecessary/slightly harmful (drop it); aggressive aug hurts in-distribution. Best recipe: resp, z-only, no aug. Harness validated to reproduce logged baselines exactly. Report: `_html/07_respiratory_variants_analysis.html`. |
| [`06_ocmr_realtime_eval_data.md`](06_ocmr_realtime_eval_data.md) | **OCMR real-time data prep** (2026-06-18): reconstructed **10 clean** real real-time free-breathing SAX cines from OCMR `us_*` k-space (R≈9) for the gated→real-time transfer test the project hinges on. Selection funnel (47→23→17→11 QC-pass→10; us_0179 excluded as FOV-aliasing), **k-t CS-SENSE** recon (ESPIRiT + PDHG temporal-TV, λ=0.02, GPU) → `scratch/data/ocmr/recon/{subject}/sax_cine.nii.gz` (4-D, ~292 MB). KEY caveats: `frame` is real-time (NOT gated/phase-aligned); k-t recon = *optimistic* eval (uses many frames/slice — cleaner than true single-shot); no clean 3-D reference exists (Tier-C/B only). 6 long stacks fail (FOV-shift TODO). **Model run** (z-only, qualitative) → `tools/eval_ocmr_inference.py` + report `_html/13`. |
| [`07_predicted_dvf_analysis.md`](07_predicted_dvf_analysis.md) | **Predicted DVF realism check** (2026-06-18): dumped the true predicted Δ in **mm** for run `t59w6nqy` (resp, z-only, no-t, aggft) over all 30 val subjects (`tools/dump_predicted_dvf.py`). Verdict: **learning useful, realistic, breathing-aware DVF** — Δz tracks applied breathing **corr +0.87**, sane magnitudes (median ~1 mm, p95 ~8–10 mm, through-plane max ~14 mm), correctly idle on near-reference slices. Ceiling: **under-corrects deep breaths** (fit slope **0.42**, plateaus ~7–8 mm vs ~24 mm) — the shrinkage of an r-blind model (headroom for r / input-t). One artifact: heavy in-plane tail (rare pixels ~45 mm, likely edge/low-coverage). Also: confirmed the resp tilt is a clean SI-dominant cone (domain randomization), and switched the DVF wandb viz to per-axis mm colorbars. |
| [`08_ood_clean_paradox.md`](08_ood_clean_paradox.md) | **Why OOD OCMR looks "cleaner" than in-dist val** (2026-06-20, reconciled with `10`/`11`): resolves the red flag that OOD recons looked better than in-distribution. **The blur is mostly the SPLAT renderer (~75%, `docs/10`), common to both — NOT breathing.** val looks worse for two reasons: (1) **resolution** makes that splat blur *visible* on high-res val (inputs 2.4× sharper) and invisible on low-res OCMR — a matched render shows breathing-OFF val is the *sharpest*; (2) **breathing** adds val's *extra* ~25% blur + dark spots (ON→OFF = **−2.31 dB motion PSNR**, primary metric; ON 19.3 below the ~20.6 dB floor, OFF 21.6 above). Black spots minor at full input + refiner-solved (`11`). Records two corrected wrong answers: "OCMR has dense coverage" (false — `/prove-it` + deep-hole metric: OCMR coverage is the *holeyest*) and "blur is all breathing" (false — ~75% is splat). Scripts: `tools/diagnose_{ood_clean_paradox,ocmr_cleaner}.py`. |
| [`09_aggft_phase_characterization.md`](09_aggft_phase_characterization.md) | **Aggregator-finetune & per-phase characterization** (2026-06-10), read on motion PSNR (identity floor ≈ 20.6 dB). **Finetuning the aggregator — not just the point head — is what corrects motion** (+2.7–3.2 dB vs +0.6 for head-only, which does ~zero at ED). **All-12-phases is free** (ties the {0,7} specialist). **z/t embeddings worth ~0.8 dB motion**; dropping them bleeds content into out-of-FOV padding (full −3 dB). **~1.7 dB mid-systole dip** (best at ED, worst at peak contraction t4). **Info-contract signal:** `no_zt` (no per-slice input-phase) still reconstructs at ~22.8 dB motion → network infers phase from images (see `04`). Reports `_html/01`+`_html/02`; script `tools/analyze_two_models.py`. |
| [`10_breathing_failure_mode.md`](10_breathing_failure_mode.md) | **Why the breathing recon is blurry** (2026-06-16, `tools/measure_sharpness.py`). Recon = 0.65× GT sharpness. **Decomposition: ~75% of the blur is the SPLAT renderer, ~25% is the breathing** — even on perfect clean inputs the splat alone drops to 0.74× GT, and the **trained model's sharpness ≈ raw-splat sharpness** (point head fixes *position*, splat caps *detail*). It's **blur, not holes** (coverage_frac 0.713 ≥ 0.69 tissue; only ~5–6% under-covered). Black input pixels are gated out (`intensity>1e-3`). **Motivates the refiner** (biggest, motion-independent lever). Report `_html/08`. |
| [`11_unet_refiner_results.md`](11_unet_refiner_results.md) | **3D UNet refiner results** (2026-06-17/18; impl in `version_history/v2_unet_refiner.md`, motivation `10`). On a **fixed geometry the refiner adds +~1.0 dB motion / +~1.5 dB bbox** (frozen, n=100) and its V_refined motion (20.07) **beats the best no-refiner baseline** (19.28 at a more-trained geometry) → **deblurring the splat beats training the geometry further**. BUT the gain is almost all **dark-spot/coverage correction, NOT deblur** (sharpness 0.668→~0.69× GT, ~6–8% of the gap). A **2D per-slice SSIM term** reaches that ceiling *faster* at no PSNR cost but **plateaus at the same ~0.69× ceiling set by the splat**. Black spots solved; blur needs an *upstream* fix. Reports `_html/10,11,12`; eval `tools/eval_refiner.py`. |
| [`12_xcaseg_repo_evaluation.md`](12_xcaseg_repo_evaluation.md) | **xcaseg repo eval — DON'T ADOPT** (2026-06-10). Despite the name, `xcaseg` ≠ XCAT: it's a synthetic **X-ray coronary angiography** segmentation data generator (course project, no license), not a cardiac-MRI/motion tool. Its only motion model is an **uncalibrated Perlin-noise cardiac contraction** (we already have real cardiac motion; **no respiratory motion anywhere**), welded to a NeRF X-ray renderer. Nothing to drop in; only confirms the "deform-then-reslice" ordering `01` already specifies. The X-ray analogue of the MRXCAT/XCAT rejection in `01 §6`. Report `_html/05`. |
| [`13_limitations_and_improvements.md`](13_limitations_and_improvements.md) | **Limitations decomposition + improvement roadmap** (2026-06-20, rev.2): training-free oracle ladder + 4-expert debate on the current best model (joint refiner ep~76), motion-PSNR primary, n=30. **Dominant lever = MOTION ESTIMATION:** ~14 dB motion gap (oracle perfect-placement 34.98 — built by splatting the true target-phase planes at Δ=0, **no DVF needed** — vs model 20.62; the refiner/SSIM work polished the wrong lever; model under-corrects per doc 07 slope 0.42). **Secondary = sharpness**, renderer-limited by the **256→518→256 RESIZE not the kernel** (native-256 splat 0.997× vs 0.768×); fix = render splat at native 256 ("518 in, 256 out"; Option 1≈Option 2, Δ round-trips at 0.61%); sharpness-only, PSNR-neutral at today's geometry, needs head re-fit. **Motion roadmap:** motion-weighted loss, low-rank/smooth Δ (B-spline/basis), aux cardiac-phase head, keep/accelerate aggregator fine-tune, oversample large-motion. **CORRECTION:** unfreezing the aggregator IS a real lever (joint ep76 20.62 overtook frozen ~20.07; grad-norm 0.014 = slow-but-real) — earlier "low ROI" claim withdrawn. **Demoted:** multi-draw (needs extra frames → against fast-acquisition goal; diagnostic only), fancy losses, DC loss (~34 dB), k-space. Domain gap (single-shot −3.03 dB) + real-data metric **deferred** (assume good input images). Report: `_html/14_limitations_and_improvements.html`. |
| [`15_mnms_nnunet_segmentation_eval.md`](15_mnms_nnunet_segmentation_eval.md) | **M&Ms nnU-Net cardiac segmentation as a recon-quality metric** (2026-06-20, validated). Pretrained M&Ms challenge winner (Zenodo `Task114_heart_mnms`, Full 2020) segments LV/MYO/RV on our SAX output → anatomical eval beyond PSNR. It's **nnU-Net v1** (koalai's `nnunetv2` can't load it); installed into a **fresh isolated `nnunet` env** (nnunet 1.7.1 + torch 2.3.1+cu121) — **`svr` untouched, bit-identical**. Model+data on GPFS (`scratch/data/nnunet_mnms/`, md5-verified). Runs 2d/3d/ensemble on GT + VGGT-pred volumes → clean LV/MYO/RV. First read (6 ED subjects): **Dice(seg(pred),seg(gt)) ≈ LV 0.94 / MYO 0.81 / RV 0.93** (ensemble). Tools: `tools/nnunet_mnms_eval/`. |
| [`14_fiss_5d_reconstruction.md`](14_fiss_5d_reconstruction.md) | **N=5 FISS free-running k-space → 5D recon** (2026-06-20, in progress). Zenodo 13868462 ships **raw Siemens TWIX only, no recon code**; reimplementing the Free-Running Framework (Di Sopra 2019). On-node inspection (no install, `twixtools` in `elastix`) fully de-risked it: 3D radial **spiral phyllotaxis**, **1877 interleaves × 24 spokes**, 20 coils, **112³ @ 2.0 mm iso**, first-spoke-per-interleave = exact **SI navigator**; PCA+FFT of navigators shows **separable respiratory + cardiac peaks ⇒ self-gating feasible**. Target: 25 cardiac × 4 resp CS recon. Engine in disposable `fiss-recon` env (`svr`/`elastix` untouched). SAX reslice handled separately. |
| [`17_cmrxrecon_target_t_phase_consistency.md`](17_cmrxrecon_target_t_phase_consistency.md) | **Is CMRxRecon `target_t=k/12` a fixed cardiac state? — NO** (2026-06-21, all 301, gold-standard). The 12 phases are a **resampling of each subject's own R–R** (native `TemporalPhase` 14–40, *never* 12 → fixed 12) ⇒ `target_t` = **normalized fractional time**, not a state. Proven exact (301/301 frames=12) + by nnU-Net Task114 LV-volume on all **301×12** volumes: EF 64±6%, **ED=frame 0 for 97%** (anchor), **ES drifts frames 3–8 / frac 0.25–0.67 / std 0.72** (robust sub-frame std 0.68 → real), cross-subject state spread ≈0 at ED → **peaks 0.20 in mid-diastole** (diastole absorbs HR variation). Bounded within-cohort (IQR t5–6) but tails reach ACDC range; **no ES label** ⇒ no ED↔ES normalization. Ignorable for CMRxRecon-only training, NOT a fixed-state claim, NOT a license to mix ACDC. Code prove-it-reviewed (1 bug fixed). Tools `tools/cmrxrecon_phase_analysis/`, report `_html/16`. |
| [`16_goettingen_radial_recon.md`](16_goettingen_radial_recon.md) | **Göttingen radial RT free-breathing bSSFP — download + RT-NLINV recon** (2026-06-21, done). Second RT/FB SAX eval source (radial complement to OCMR `06`). Downloaded **68 volumes** (209 GB, 5 Zenodo parts) and reconstructed each with **classical RT-NLINV** (authors' BART pipeline `mrirecon/nlinv-net`, **no NN** — avoids learned-prior confound) → 68 magnitude SAX cines `(160,160,Z=20–27,127)` in `scratch/data/goettingen/recon/`. BART built from source w/ CUDA (`reference_bart_build_greatlakes`); pipeline prove-it-reviewed (2 bugs fixed: relative-path CWD, `micromamba` not on batch PATH). `nlmeans` skipped (cosmetic, 1.37% diff). **Measured respiratory motion** (authors don't report): heart-ROI in-plane **AP ≈ 1.45 mm** (gentle/shallow breathing; SI dominant but through-plane/unmeasurable) — overturned the "whole-FOV underestimates" guess (chest wall moves more than heart). Slices independently acquired → genuinely **respiratory-misaligned** (the real scattered acquisition). Eval = **self-consistency / leave-one-slice-out** (no clean 3D GT). Tools `tools/goettingen_recon/`; data-side `scratch/data/goettingen/README.md`. |
| [`18_joint_refiner_breaks_ood.md`](18_joint_refiner_breaks_ood.md) | **Joint refiner training degrades OOD generalization** (2026-06-22, qualitative, n=5/mode). Visual 3-ckpt comparison (OLD pre-refiner `218747856` / bpblrai2 frozen-bb SSIM-refiner `218246076` / NEW joint-refiner `218349151`) across val ON / val OFF / OCMR / Göttingen. Three findings: (1) the backbone alone generalizes well to OOD; (2) **joint training delegates motion-correction to the refiner — even the NEW model's V_canon is messier than OLD's** → backbone itself is degraded by joint training, not just the refiner; (3) under large motion, V_canon is input-draw-dependent (motivates multi-draw ensemble from `13`). Caveat caught on review: bpblrai2's "frozen backbone" is the SAME run as OLD (`t59w6nqy = 218747856`) but at an *earlier* epoch (seed-file Jun 16 19:01 vs OLD `checkpoint_last` Jun 17 22:17, per-tensor diff 1.51e-2), so Row 1 ≠ Row 2 in `bpblrai2_*.png` is the snapshot mismatch, not a sanity-check failure. Recommendation: for OOD, prefer OLD backbone + separately-trained refiner over joint training. Scripts: `tools/{three_ckpt_compare,bpblrai2_compare,diagnose_refiner_question,render_oldckpt_4way,random_draws_multi_subj}.py`. |
| [`19_motion_correction_warp_ceiling.md`](19_motion_correction_warp_ceiling.md) | **Motion correction is warp-architecture-limited** (`/toy-experiment`, 2026-06-22): directly optimizing Δ on the real task (no network) = the **warp-only ceiling ~21 dB ≈ the trained model (20.6)**, ~14 dB below the oracle (35). The model is already at its architectural ceiling; the 14 dB is the **appearance wall** (warping other-phase intensities can't synthesize target-phase appearance), NOT motion estimation. ⇒ best fix = **appearance synthesis (learned renderer/decoder)**; loss/low-rank-Δ/unfreeze/conditioning have ~0 headroom vs the warp ceiling. Secondary: splat coverage-division weakens the through-plane gradient ~2× (E0); inverse-warp/no-cov-div restores it. **prove-it caught + reversed an lr=0.03 Adam-divergence bug** (L1 IS aligned with motion). **Corrects doc 13.** Report: `_html/18_motion_correction_warp_ceiling.html`. |
| [`20_appearance_synthesis_test.md`](20_appearance_synthesis_test.md) | **Appearance synthesis is NOT a breakthrough** (`/toy-experiment`, 2026-06-22): tested doc-19's proposed fix (splat *features* + 3-D decoder to synthesize target-phase appearance). **Refuted, conclusively (prove-it clean):** feature-splat decoder adds **+0.03 dB** over transport on held-out; recoverable-appearance ceiling is **~28 dB even with DENSE adjacent-phase volumes** the model lacks; **population template useless (14.4, below floor)** — hearts unaligned in canonical grid ⇒ no free prior. Synthetic toy: synthesis recovers population-predictable appearance, **hallucinates** subject-specific. Mechanism: ~7-9 dB of the gap is subject-specific appearance NOT in the sparse r/t-blind inputs — no renderer synthesizes absent info. **⇒ real lever = relax the INPUT CONTRACT** (more frames / input-t / 5-D ref), not the renderer. **Corrects doc 19 §5 prescription** (diagnosis stands). Report: `_html/20_appearance_synthesis_test.html`. |
| [`21_motion_psnr_contract_levers.md`](21_motion_psnr_contract_levers.md) | **Motion PSNR is acquisition-coverage-limited; the only free lever is proximity sampling (+0.8 dB)** (`/toy-experiment`, 2026-06-22): tested doc-20's "relax the contract" prescription, training-free (rebuild input compositions; no retrain), prove-it-clean ×3. **(A) Count:** adding input frames *at* the target phase raises *full-volume* motion PSNR +2 dB but it's a pure **observation leak** — leak-free **held-out-z** PSNR *falls* 18.2→13.6 ⇒ the model fills only planes it directly measures, does **NOT propagate** target appearance to unobserved planes. **(B) Proximity:** near-target companions (t±1) beat random by **+0.8 dB** at fixed budget; warp-ceiling gain **+0.84 dB** ⇒ model already at its proximity ceiling, **no retrain headroom**. Everything else ≤0.1 dB (renderer/decoder/splat-res/input-t). **⇒ The ~21 dB wall is an ACQUISITION-COVERAGE wall:** free win = proximity sampling (~+0.8); a *large* gain requires **breaking the fast-acquisition contract** (more/closer target-phase frames, temporal stream, 5-D ref) and even then helps only measured planes. **Completes doc 20.** Report: `_html/21_motion_psnr_contract_levers.html`. |
| [`22_reference_slice_amplitude_vs_motion_psnr.md`](22_reference_slice_amplitude_vs_motion_psnr.md) | **A target-phase REFERENCE slice fixes contraction AMPLITUDE (EF), NOT per-voxel motion PSNR** (`/toy-experiment`, 2026-06-22): tested the user's "give it the LV state I want" idea + reconciled "more frames don't help" (docs 20/21) vs "target-phase obs recovers EF" (flat-EF). Real model, no retrain, leak-controlled, prove-it-clean ×2. **(1)** Reference slice recovers per-patient amplitude — toy pred-EF slope **0.26→0.73** (input-t labeling does nothing); prior coverage ablation →1. **(2)** But held-out per-voxel motion error is appearance **PATTERN** not magnitude — per-plane cosine **0.44**; even a PERFECT amplitude correction (per-plane oracle) adds **+0.25 dB**, realistic 1-plane **HURTS −0.3**; **98%** of the error isn't amplitude; holds every phase (0/100 samples >1 dB). **Reconciliation:** EF is volume-integrated/observed-plane-dominated; per-voxel motion PSNR lives on unobserved-plane appearance (doesn't propagate, doc 21; info-limited, docs 19/20). **⇒ DIRECTION: Option-B reference conditioning to fix EF/amplitude (clinically meaningful, the model's real fixable error); per-voxel motion PSNR is acquisition-limited; reconsider it as the headline metric.** Report: `_html/22_reference_slice_amplitude_vs_motion_psnr.html`. |
| [`23_ood_eval_adapter_refactor.md`](23_ood_eval_adapter_refactor.md) | **OOD RTFB inference consolidated into a permanent `eval/` package** (2026-06-23, done, prove-it-clean). Replaced the scattered, asymmetric `tools/` adapters with a `BaseRTFBAdapter` (3 seams: `load`/`inplane_mm`/`slice_positions_mm`) over one shared canonical pipeline, plus shared `inference.py` (`load_rtfb_model`/`forward`/`phase_sweep`), `render.py`, and one `run_rtfb.py` runner (`--dataset {ocmr,goettingen,miitt}`). **OCMR bit-identical** (pure parameter-threading, guarded by `tests/test_eval_ocmr_equivalence.py` vs a frozen snapshot); **Göttingen intentionally better** (native slices, drops the old 6→8 mm through-plane interp); **MIITT new** (placeholder spacing, qualitative; its 5-row panel cross-validates `18` on a 3rd dataset). `tools/eval_ocmr_inference.py` is now a thin back-compat shim; 5 comparison scripts rewired; deleted the 129 MB placeholder `canonical_subjects/` + obsolete `goettingen_to_canonical.py`/`goettingen_infer.py` (real recon untouched). No training/`MRIDataset` code touched. 170 tests pass; all 20 real subjects build. Adding a future RTFB dataset = one new subclass. |
| [`24_flat_ef_amplitude_regression.md`](24_flat_ef_amplitude_regression.md) | **The model reconstructs a flat ~48 % EF for everyone** (2026-06-22, cause chain PROVEN). Over 30 val subjects × 12 `target_t`, nnU-Net-segmented EF: the model gets cardiac **timing** right (ES ±1 frame, 93 %; curve_corr 0.93) but regresses contraction **amplitude** to the cohort mean — pred-EF 47.96 ± 9.23 % vs true 62.58 ± 7.37 %, slope ≈ **0** (−0.03), 14.6-point gap; a true-92 % heart reconstructed at 35 %. **Proven cause:** NOT the pipeline (oracle-splat of true slices through the same splat+nnU-Net → slope **1.03, r=1.00**); the **model**, and it's an **information/observation limit** (architecture sound) — a coverage ablation forcing `k` slots to observe the target phase flips the slope **k=0 −0.03 → k=6 1.03**. Mechanism: scattered one-frame-per-slice inputs under-observe the target phase ⇒ L1-optimal answer = population-mean contraction. This is the **amplitude half** of the `target_t`-index problem; fix in `25`. Data `scratch/analysis/phase_analysis/`, tools `tools/cmrxrecon_phase_analysis/`, report `…2026-06-22_1421_vggt-ef-amplitude-regression.html`. |
| [`25_target_phase_reference_design.md`](25_target_phase_reference_design.md) | **One design change fixes BOTH `target_t` problems: index → target-phase reference slice** (2026-06-23, design decision, not yet retrained). The unified synthesis: conditioning on a content-free `target_t` **index** is the common root cause of **Issue 1** (timing — `k/12` is fractional time not a fixed state, drifts across patients, unsafe to mix cohorts; doc `17`) AND **Issue 2** (amplitude — index carries no per-patient info ⇒ flat-EF regression to cohort mean; doc `24`). Replacing it with a target-phase **reference slice** (the *original* VGGT camera-token design — special token carries an observed image, not a code) dissolves both: the image defines the phase per-patient (Issue 1 gone + **cross-dataset mixing becomes safe**, reference = universal state coordinate) and carries patient-specific content (Issue 2 gone, EF recovers). Support: toy slope **0.26→0.73**, real-model ablation **0→1**. Caveats: reconstructs *observed* phases (≈ recoverable limit); does NOT move per-voxel motion PSNR (doc `22`). **Needs a retrain to confirm** (implementation sketch in §5). |
| [`26_single_phase_aggft_vs_headonly.md`](26_single_phase_aggft_vs_headonly.md) | **Aggregator-finetune beats head-only in BOTH single- and multi-phase** (2026-06-23, motion PSNR @ED, n=30). **Multi-phase:** head-only ≈ identity floor (20.45 vs 20.13, no motion correction); aggft **+3.4 dB** ⇒ required. **Single-phase (ED-only):** head-only is **NOT** at the floor — recovers **+2.6 dB** (22.79 vs 20.15), **correcting `docs/09`** whose "head-only ≈ zero at ED" was *multi-phase-specific* (a frozen head can memorize ONE fixed-target warp but not 12 target-conditioned ones); aggft still wins **≥+0.56 dB** (lower bound — aggft beat near-converged head-only at only **28 % trained**, so both jobs were stopped early; converged magnitude unmeasured by choice). **Why aggft helps even single-phase:** inputs are *always* scattered across phases (`mri_data_mode: dynamic`), so fixing the target query doesn't remove the warp task. Methodology: first single-phase run was discarded as **undertrained** (killed ep62/200, LR ~82 % of peak); fair re-run = z-only/no-t, no breathing, 100-ep fully-annealing, identical except freeze. **Also:** fixed the trainer so single-phase runs now log `val_motion` (was gated to multi-phase). Don't cross-compare t-on vs t-off absolute dB. Scripts `tools/eval_ed_motion_{aggft_vs_headonly,fixedED_z_no_t}.py`; runs `sbatch/train_fixedED_z_no_t_{headonly,aggft}.sh`. |
| [`27_slice_spacing_thickness_vs_pitch.md`](27_slice_spacing_thickness_vs_pitch.md) | **Canonical Z = 12 mm: thickness vs. true pitch** (2026-06-22, done). CMRx/OCMR recon NIfTIs were stamped Z=8 mm = slice *thickness*; CMRx's true pitch is **8 + 4 mm gap = 12 mm**, so the cube was **Z-squished to 67 %**. Fix: **relabel the affine on disk** (`tools/relabel_slice_spacing.py`, header-only, reversible) to each dataset's true pitch — **CMRx→12**, **OCMR→measured 9.6/10** (ACDC 5/10 & Göttingen 6 already honest, untouched) — and set `TARGET_SPACING` Z (training) + `CANON_Z_SPACING_MM` (inference/adapters) to 12. CMRx affine now genuinely 12 ⇒ `Spacingd→12` is a Z-**identity** ⇒ cached tensor byte-identical ⇒ **no retrain**. Voxels never touched. Only respiratory mm→voxel calibration and eval gridding actually change. 183 tests pass. Verify imgs `result/slice_spacing_verify/`. |
| [`28_multiframe_and_continuous_z.md`](28_multiframe_and_continuous_z.md) | **Multi-frame-per-slice input + continuous physical z** (2026-06-30, implemented + test/smoke-validated, **gains not yet confirmed by a real run**). **(A) Multi-frame (default ON, S=20):** replaced one-frame-per-plane with a fixed S=20 set = **full z-coverage + uniform-random extra frames** (multi-frame; closer to real acquisition + VGGT's many-view training). Rationale: a per-slice *cine* makes through-plane Δz + contraction amplitude observable **from content** (no phase label, no flat-EF; docs 24/25/22) — the principled fix for the starved-Δz fork. Fixed S avoids fragmentation; full coverage keeps the full-volume L1 loss valid unchanged. **(B) Continuous z (default OFF, gated `continuous_z`):** ±0.5 jitter off the integer plane + 2-plane interpolation teaches z as a continuous physical coord so off-grid inference slices splat correctly (a train-time aug; valid because the gated source volume is coherent — can't resample free-breathing slices). Touch points: all sampling in `get_data`; `slice_indices`→float32 through collation + the one `gpu_aug` gather upgraded to interpolate (respiratory already float-safe); config budget 12→20. **Untouched:** loss/model/splat/confidence; `t` is extraction-only. Measured A40: S=20 ≈ 37 GB, no creep. 205 tests pass. **Next:** real multi-frame training run to confirm Δz/EF gains, then ablate `continuous_z`. Deferred: reference-cine-plane flag, varying-pitch, confidence-weighted splat. |
| [`29_niftymic_baseline_first_run.md`](29_niftymic_baseline_first_run.md) | **NiftyMIC baseline — first real end-to-end run** (2026-07-02, n=2, proof-of-concept). Ran the classical SVR toolkit from `_html/24_svr_baselines.html` on 2 val subjects' real clean per-phase (t=0) SAX stacks. **Works: corr 0.886/0.901 with true anatomy, PSNR_anat 23.6/25.3 dB (intensity-calibrated)** — confirms the reconstructed structure is real, and empirically confirms the report's "little to register" single-orientation caveat. **PSNR_full stays low (~7 dB)** — NiftyMIC's 50 mm reconstruction-space padding spills into our zero-padded canonical background; use `_anat`, same convention as the project's own `_bbox`. **Three real bugs found + fixed** (now in `baselines/niftymic/`): (1) Singularity bind-mount to a deep host path silently fails — bind to a short mount point instead; (2) the shipped image has an unconditional SimpleITK API-drift bug (old positional-args `Execute()` NiftyMIC was written against, removed in SimpleITK 2.x) — patched via a bind-mounted fixed copy of the one broken file; (3) NiftyMIC's Tikhonov SRR doesn't preserve absolute intensity scale (`pred ≈ 1.03·gt + 1.0`) — score with a linear intensity calibration, standard in the SVR literature. Runtime ~18–25 min/subject, CPU-only, LSMR didn't fully converge at the default 10-iter cap. Next: NeSVoR, SVRTK, more subjects. |
| [`30_baseline_testing_strategy_and_corrections.md`](30_baseline_testing_strategy_and_corrections.md) | **Baseline testing strategy + corrections** (2026-07-02, continuation of doc 29). Verified facts: neither NiftyMIC nor SVRTK self-gates in any mode (only `fetal_cmr_4d` has that code); SVRTK's "cardiac mode" is velocity/4D-flow-only, unusable for magnitude cine; **our respiratory sim is a PURE RIGID translation** (verified from code — corrects an earlier in-session claim it was deformable); CMRxRecon's raw `ChallengeData` has real SAX+LAX+LVOT multi-orientation cine sharing the same gated phases (cheap, high-value, not yet built). **Real bug found:** the VGGT-MRI eval used `num_slices=20` but the live checkpoint started training 6 days before the multi-frame commit landed, so it actually trained with `num_slices=12` (one frame per z-plane, no repeats) — the eval was silently out-of-distribution; fix applied, corrected number pending. Records 3 false-start test designs and why they were wrong, plus the corrected fair-comparison protocol (same-phase respiratory-corrupted stack for classical SVR vs VGGT's native sampling) not yet executed. |
| [`31_baseline_roster_and_single_stack_decision.md`](31_baseline_roster_and_single_stack_decision.md) | **Updated baseline roster + single- vs multi-stack decision** (2026-07-02, synthesis of `24`/`29`/`30` + design discussion). **Four runnable SVR baselines** — NiftyMIC (run), SVRTK (rigid, EM-robust, standard citation), NeSVoR (the *only* usable ML SVR, **now run** — see `32`), fetal_cmr_4d (the *only* self-gating end-to-end 4D pipeline) — plus a stack+interp floor; **DMCVR dropped** (interpolation on a coherent stack, not SVR). Central fact: **single-orientation through-plane reconstruction is mathematically underdetermined**, so *every* SVR tool degrades to through-plane interpolation on single SAX — it's an input limit, not a tool flaw, and exactly the gap VGGT's learned prior fills (trade acquisition redundancy for a cohort prior). Decision: run classical SVR **single-stack** (matched input — its interpolation-degradation is the evidence the prior works) **AND** multi-stack (its designed best, only possible on gated CMRxRecon SAX+LAX+LVOT with GT) as an anti-strawman upper bound; keep the through-plane claim (gated) separate from the free-breathing claim (no GT). All real FB data (OCMR/Göttingen/MIITT) is **structurally SAX-only**; multiplanar+free-breathing+GT doesn't exist in the catalog. Also: self-gate→SVR decomposition for real-time, timing breakdown (the 6 h is per-phase recon + manual masking, not gating), n=2 clean-stack run measures round-trip loss not motion correction, and honest contribution scoping (EF/timing recoverable, per-voxel motion PSNR acquisition-limited, real-time transfer still simulated). Experiment plan in §8. |
| [`32_nesvor_baseline_first_run.md`](32_nesvor_baseline_first_run.md) | **NeSVoR baseline — first run + respiratory-corrupted re-run** (2026-07-04, n=2). **Clean-stack protocol** (same as `29`): NeSVoR beats NiftyMIC clearly — corr 0.981/0.965 (vs 0.886/0.901), PSNR_anat calibrated 32.00/30.28 dB (vs 23.59/25.28 dB, +6.7 dB mean), faster (~8.5-9 min/subject GPU vs NiftyMIC's 18-25 min CPU) — but this measures pipeline round-trip fidelity, not motion correction (input IS `V_gt`, nothing to correct; both tools share the same single-orientation through-plane limit, `31` §3). **One real bug found+fixed**: NeSVoR's default internal sampling-mask estimation (voxelize+blur+threshold, tuned for denser multi-stack fetal-brain point clouds) comes out all-`False` on our single-stack ~8.6:1 anisotropy → crashes late in `Volume.resample()`; fixed via the documented `--sample-mask` escape hatch (pass our own `content_mask.nii.gz`), bypassing the internal estimator — will recur for any future single-stack NeSVoR run, always pass it explicitly. **Respiratory-corrupted re-run (the real test, docs 30/31):** built `baselines/export_resp_stack.py` (per-z-plane rigid shift via the trainer's own live `RespiratoryConfig`); mean corr drops 0.973→0.859, PSNR_anat drops 31.14→22.99 dB (**-8.15 dB**) — a real, non-trivial degradation, the first baseline number that actually reflects motion correction. VGGT-side comparison deferred (open question: no checkpoint is both reference-conditioning AND trained under the S=20/multi-frame or deployment-realistic sampling regime — see doc body). |
| [`33_4wok_conclusive_analysis.md`](33_4wok_conclusive_analysis.md) | **Conclusive per-axis analysis of model 4wokxzov** (2026-07-04, n=30, 4-agent-debated). Reference-slot / DPT / 1-frame model, trained with an L2 DVF-smoothness ("diffusion", NOT generative) regularizer. **EF is RECOVERED** (slope 0.77 / Pearson 0.765, but honest robust ≈ **Spearman 0.55 / leak-excluded 0.68** = recovers ~half the per-patient EF spread with mild under-contraction; PROVEN learned-not-copied via reference-plane exclusion) — **corrects earlier in-session "flat EF" pessimism** (that was an undertrained checkpoint; L1-TV also recovers EF at final, slope 0.79, so the **reference-SLOT drives EF, not the regularizer/head**). **In-plane cardiac motion real & localized** (mean 0.5 diluted; p95 2.87, max 6.7 mm); **through-plane cardiac minimal** (p95 0.49) but low-impact (LV volume is in-plane-dominated). **Breathing partial** (slope 0.35, deep breaths ≥12 mm → only ~5.7 mm, ~54 % ignored, residual ~1.9 dB in a ~12 % tail; cause split renderer-vs-blind-r). **~7–9 dB appearance gap = info-limited contract price** (docs 19–21). Head second-order (bspline 3× more through-plane, same PSNR = cosmetic). **Bugs caught + fixed:** eval missing `reference_slot=True` (artifactual "no motion"), broken `recovery_pct` stat, floor-mislabeled-as-ceiling, nnU-Net silent-fail (needs `-tr nnUNetTrainerV2_MMS`). Fixes: more reference planes for EF (doc-24 ablation slope→1 at k=6); coverage-free renderer for shallow/mid breathing (deep tail info-limited); proximity sampling (+0.8) for the appearance gap. Report `_html/33`; scripts `tools/exp_4wok_analysis.py` + `exp_4wok_p95.py` + `render_4wok_qualitative.py`. |
| [`36_svrtk_baseline_selfgating_and_recon.md`](36_svrtk_baseline_selfgating_and_recon.md) | **SVRTK baseline — comprehensive operational reference: self-gating + 3D/4D reconstruction** (2026-07-07). End-to-end how-to for the classical-SVR baseline on single-orientation SAX RTFB (self-gate → reconstruct), the companion to doc 34 (why fetal_cmr_4d fails single-orientation) and doc 35 (self-gating *method research*). **Two recon engines, both roster baselines:** SVRTK **4D joint** (`reconstructCardiac`, temporal PSF, ~4.5h/33GB on 4 cores → ~40-70min on 32) — temporally smooth but **flattens the beat**; SVRTK **3D per-phase** (`mirtk reconstruct`, one static SVR/phase, ~6min parallel) — **preserves the beat but noisier**. **Headline finding (measured):** the 4D-joint **under-contracts** (LV EF ~9% though raw input contracts ~57%) and the cause is the **temporal PSF** (blends ED/ES) — NOT breathing (motion corrected, LV position stabilized 8mm→1.5mm), NOT sync (well-synced slices flatten), NOT resolution; proven by a phase hard-bin of the input preserving 46% + the 3D-per-phase recon recovering it. Self-gating validated on 2 subjects (ED scatter 75/122° → ES 15.6/6.7°). **Includes an exhaustive Problems & Gotchas §9** (trailing-space arg bugs, 32GB OOM though real peak ~33GB not 128, nnU-Net-vs-recon CPU contention, `pkill -f` self-kill, per-stack `-thickness`, background-task deaths, robust-stats excluding 19-33% of slices, base/apex no-LV, RT LV-centering) — READ before running. Code `baselines/fetal_cmr_4d/`; DEVIATIONS.md §E. |
| [`35_self_gating_methods_for_svr.md`](35_self_gating_methods_for_svr.md) | **Self-gating methods to feed SVRTK on single-orientation SAX RTFB** (2026-07-05, research + design, nothing implemented). Deep sweep (21 sources, 25/25 verified) to turn ungated SAX real-time cine into per-frame cardiac phase + per-slice R-R for `reconstructCardiac` (doc 34), image-domain only (magnitude/complex, no k-space/navigator). **Decision: two jobs, two methods** — per-slice **R-R period ← x-f temporal-Fourier FFT** (van Amerom/fetal_cmr_4d, already in-repo, robust at every slice level), per-slice **ED anchor ← LV blood-pool AREA** (Åkesson CPFI 2025, the one on-point SAX RTFB paper; ED = area local-max ±3, ES = area min between EDs) via our public **nnU-Net Task114** (Åkesson's Bai-2018 weights private, no code). Assemble continuous θ=2π·frac((f−f_ED)/T) → slice-major `-cardphase`; anchoring every slice's ED to θ=0 **is** the cross-slice sync (no overlap needed → substitutes for the interslice step SAX breaks). **Sharp reason LV-area is the best anchor:** all other methods (Fourier phase, manifold, PCA/SSA, intensity) give a *periodic signal* that can't say which phase is ED — LV-area is the only surrogate *anatomically ED by construction*; that's what breaks doc-34's **106° scatter**. **Complex does NOT help** (Seo 2017 phase-gating is a velocity nav, not image phase) → magnitude is the default. **Base/apex = PHYSICAL limit** (AV-plane descent + vanishing apex cavity), not a method choice — no method fixes it; down-weight via SVRTK robust stats. **Classic canon (Larson/SSA-FARY/XD-GRASP/ICA) = cite-able background but unusable** (needs k-space/navigator). **One open choice, reduced to a measurement (2-agent debate converged):** SVRTK's temporal PSF replaces Åkesson's image resampling (NO to resampling); whether linear θ lets ES drift → measure cross-slice **ES-phase scatter** (ES = LV-area min, same masks); default **linear θ**, upgrade to **two-anchor piecewise-linear θ labels** only if scatter ≫15°. Next: build gater + validate the 106°→<25° collapse on one MIITT subject before SVRTK. |
| [`34_fetal_cmr_4d_methodology_and_single_orientation_findings.md`](34_fetal_cmr_4d_methodology_and_single_orientation_findings.md) | **fetal_cmr_4d — methodology, MIITT adaptation, and why it fails single-orientation** (2026-07-05, comprehensive record). The only self-gating classical 4D pipeline (van Amerom MRM 2019); turns ungated multi-orientation real-time 2D slices into an isotropic 4D cine via self-gating + rigid moco + 4D super-resolution (SVRTK `reconstructCardiac`, joint 4D + temporal-PSF; MATLAB `cardsync_*` does the gating). **Decisive finding: it fundamentally does NOT work on single-orientation (SAX-only) data**, for TWO reasons from the missing multi-orientation overlap — (1) through-plane resolution only interpolated (blur); (2) **cross-slice cardiac sync impossible** — interslice sync aligns slices via 3D overlap that only exists across orientations, **empirically proven on MIITT V1: per-slice ED phase scatters with circular std = 106°** → the 3D LV blends ED/ES states → the recon (and `_html/27`) is a **compromised** result, not "coherent, just blurry". **Fixes:** add a real-time LAX (2 orthogonal stacks) = the FAITHFUL fix (LAX crosses every SAX slice → gives sync its overlap AND fills through-plane; needs RT-not-gated LAX, MIITT is SAX-only → needs it from Jesse); OR a per-slice ED self-gate = OUR modification (sync-only, not blur; confirmed feasible — all 12 slices cardiac-SNR median 0.74, but needs a cardiac bandpass, 3-4 slices respiratory-drift-dominated). Also: fetal harder than adult (maternal + unpredictable fetal bulk motion + no ECG); heart/chest ROIs **auto-generated from cardiac-band spectral power** (paper does manual MITK); RT↔gated are separate scans (no voxel correspondence → PSNR/SSIM meaningless → use EF, but EF needs sync fixed first); all CPU; interactive salloc capped 32G. Code in `baselines/fetal_cmr_4d/` (upstream repo untouched); per-change list in `baselines/fetal_cmr_4d/DEVIATIONS.md`. **Status: not a valid single-orientation baseline as-is — decision pending (report limitation / get RT LAX / add self-gate / drop).** |
| [`34_breathing_z_fix_brief.md`](34_breathing_z_fix_brief.md) | **Breathing through-plane (SI) correction — mechanism + implementation HANDOFF brief** (2026-07-04, design brief, not implemented). Deep-dive on the breathing-z limitation for an executing agent. Baseline: DVF slope 0.35, ~54% of deep breaths ignored, ~1.9 dB residual (a ~12% tail; secondary metric — EF unaffected). **Honest ceiling ~slope 0.5, NOT 1.0**, factored from E0: coverage-division **1.55×** (renderer-fixable) × 12mm slice-pitch **1.39×** (fundamental) × blind-inference info floor (corr 0.52). The loss is a *drag not a blocker* (full-volume L1 penalizes holes, but only 1.55× damping; direct-opt reaches slope 0.44 through it; do NOT coverage-mask — degenerate). Sim breaths are **per-plane iid = differential** (`respiratory.py:146`), so recoverable from cross-slice coherence — the blind 0.52 is an under-exploited-coherence limit, not a wall. **Ranked levers** (all RETRAINs): (B) coverage-free **auxiliary placement loss** via `sample_volume` gather [best ROI, add-a-loss] → (A) coverage-free renderer retrain → (C) oversample deep breaths → (D) explicit consistency [subsumed by B] → (E) LAX/navigator [big gain, breaks contract, defer]. Expected: +0.5–1 dB / slope 0.35→~0.45–0.5, **unproven** (Step-1 coverage-free probe was degenerate — always keep splat-L1 primary, validate on DVF slope not PSNR). Two training-free GO/NO-GO gates + full file/function inventory in the doc. |
| [`36_roadmap_future_enhancements.md`](36_roadmap_future_enhancements.md) | **Roadmap / parking lot for not-yet-implemented follow-up work** (2026-07-07, moved out of `CLAUDE.md`). Headline direction = realistic real-time acquisition sim (bSSFP transient, single-shot artifacts, through-plane motion) on top of the current scattered-sampling + in-plane-aug sim. Also: Option-B continuous-phase query, blind-input-phase contract (doc 04), free-breathing respiratory embedder, phase-recovery fallback (input-phase dropout), fully-unsupervised loss (drop `gt_target_volume`), UNet refiner/ablation on the splat, and tagging k-space as in-plane motion GT. Each entry lists its own blockers; none are committed. |
| [`37_stopgrad_denominator_and_gather_aux_test.md`](37_stopgrad_denominator_and_gather_aux_test.md) | **Stop-grad denominator REFUTED; gather-placement auxiliary CONFIRMED** (2026-07-07, toy-experiment, prove-it-audited). Conclusive test of doc-34's coverage-free breathing-z levers. **The one-line `V=acc/(cov.detach()+eps)` does NOT work as a drop-in:** it restores the through-plane gradient (flat covdiv plateau→smooth ramp, 36×, forward byte-identical) and *transmits* (sighted breath slope 0.36→0.66, refutes Adam-washout) **BUT craters reconstruction — heart PSNR 21.2→16.5 dB, ~4 dB below the do-nothing floor** — by tearing coverage holes in the heart (22%→33%); coverage-division's "damping" is the *correct* gradient of a forward that legitimately penalizes holes. **The fix = gather-placement AUXILIARY (lever B):** keep covdiv splat-L1 primary + add `λ·|sample_volume(V_gt,world+Δ)−input|` (coverage-free per-pixel depth signal), slope 0.40→0.90 at heart PSNR ~21 (λ≈0.2–0.5). **Simple to implement** (~15 lines, mirrors the existing `diffusion_weight` term — 4 wiring points), **one wrinkle:** V_gt is the target phase but input slots are arbitrary phases → moving heart won't match (only phase-invariant static tissue does); breathing is a rigid whole-slice shift recoverable from the static majority, but consider masking the aux to low-cardiac-motion voxels. **Needs a short warm-start finetune** (loss change ⇒ training-only, not inference-time, not from scratch); prize ~0.5–1 dB secondary, EF unaffected. Report `research-reports/2026-07-07_0912_stopgrad-breathing.html`; code `scratchpad/toy1..exp4`. |
| [`38_gt_referenced_val_metrics.md`](38_gt_referenced_val_metrics.md) | **GT-referenced val metrics to quantify targeted improvements** (2026-07-07, implemented + verified, 208 tests pass). Fixes "even if a change helps, aggregate PSNR won't show it." 2-agent debate (process-DVF vs outcome-reconstruction) → log BOTH, strict split: outcome metrics decide whether a change ships, process metrics attribute why. Added val-only `metric_*` keys in `training/loss.py`, registered in `scalar_keys_to_log.val` (`Val_Loss/metric_*`): **`recov_frac_heart`**=(MSE_id−MSE_model)/(MSE_id−MSE_oracle) on the cardiac-motion ROI — oracle=splat TRUE target-phase content at Δ=0, **rescales the appearance wall out** so small fixable gains are visible (=1 ceiling, <0 below floor); `psnr_3d_static` (flat control) vs `psnr_3d_motion` (heart); `hole_frac_heart` (coverage<0.5 tripwire that caught stop-grad); breathing `resp_{slope,corr}_dz`+**`resp_epe_dz_mm`**+`resp_frac_deep_ignored` (predicted Δz vs exact `resp_disp_mm`, `exp_4wok_analysis.py` online). **Decision rule:** win iff recov_frac↑ & psnr_motion↑ WITHOUT hole_frac↑. Heart ROI = `compute_motion_mask` (no segmentation/universal mask needed); real EF/Dice stays offline nnU-Net. Val-only via `not pos_pred.requires_grad` ⇒ training bit-identical; try/except-wrapped; +2 splats/val-sample. Design test = must flag the docs/37 stop-grad regression, and does. |
| [`39_whole_heart_masks_and_cardiac_phase.md`](39_whole_heart_masks_and_cardiac_phase.md) | **Whole-heart seg + ROI + cardiac-phase (ED/ES/EF) across ALL datasets** (2026-07-11). One consistent anatomy-derived segmentation, heart-ROI, and GT ED/ES/EDV/ESV/EF reference for CMRx/MIITT/OCMR/Goettingen(+ACDC), so SVR baselines and VGGT are scored on the same region. **nnU-Net M&Ms Task114 (2D)** → per-frame 3-class **LV/MYO/RV** seg + a dilated **ventricle-centered** ROI (NOT atria/vessels — no such class) + (gated only) ED/ES from the 3D LV-cavity-volume curve. Stored as **siblings** next to each recon (`heart_seg.nii.gz` 4D + `heart_roi.nii.gz`; CMRx also `_canonical`) + central regenerable `scratch/data/whs/cardiac_phase.csv`. **421 units done** (CMRx 301, MIITT 26, OCMR 25, Goettingen 69) + **ACDC 150** as an SVR-recon target (job 53338428). **Task114 works on all gated + MIITT-RT + Goettingen-RT, fails only OCMR-RTFB** (aliased recon, flagged; 5 low-quality units total). **2D ≈ ensemble** on thick anisotropic slices (0.896 vs 0.902 Dice; through-plane useless at 8–12 mm pitch → 2D chosen for speed + method-consistency). EF clinically realistic (CMRx 64% / MIITT 60% / OCMR 61% mean) and **Task114-EF matches ACDC manual EF at r=0.962** — bias is common-mode in method-matched eval so it **cancels** → use Task114 everywhere, manual GT not needed operationally (manual only wins absolute high-EF HCM). **ROI recipe alignment-driven:** gated = static union-over-phases + z±1; RTFB = **per-frame in-plane only** (frames resp-unaligned, slices independent cines). **EDV/ESV = 3D Simpson's** with slice *pitch* (not thickness); **EF is spacing-independent** (robust; MIITT abs-mL provisional). **Finding:** CMRx ES-timing quantized+late (~0.54 cycle-frac) vs MIITT/OCMR ~0.37 — likely 12-frame resampling (hypothesis), so ED/ES **indices** need cycle-fraction normalization cross-dataset. **Gotchas:** micromamba cache-lock race silently dropped 16 units (fixed: `mrun()` retry + surfaced failures); CMRx canonical import pulled `vggt` (fixed: standalone `importlib`); ACDC spacing in header not affine + LV=**3** not 1. Code `tools/nnunet_mnms_eval/{make_whs_worklist,prep_one,assemble_whs,build_heart_roi,compute_cardiac_phase,plot_cardiac_stats}.py` + `sbatch/whs_segment.sh`; figures `result/whs_check/`. |
| [`40_svrtk_single_stack_motion_nonrecovery.md`](40_svrtk_single_stack_motion_nonrecovery.md) | **PROOF: single-stack SVRTK cannot recover valid per-slice motion** (2026-07-13, on the `scratch/eval/` frozen bundles). Classical SVR does not *correct* the simulated breathing, it *bakes it in* (mechanistic cause of the −10 dB breath-PSNR drop). Four-part proof: **(0)** scorer sanity (synthetic perfect→slope −1, zero→0); **(1)** controlled single-stack calibration — inject KNOWN per-plane 3-axis displacement, regress SVR's `.dof` estimate → **slope≈0/corr≈0 on ALL axes** (z through-plane AND x/y in-plane), n=12; **(2)** positive control — 2 overlapping stacks + known +6 mm in-plane shift → SVR recovers **96–103%** → the pipeline works & the failure is the **redundancy limitation, not a bug**; **(3)** spurious motion on clean — SVR hallucinates through-plane |tz|=2.5 mm mean (max 28) where there's none. **Gauge caveat:** raw `.dof` isn't comparable (SVR's consensus frame is a free gauge) → compare **demeaned** transforms (`demean(T_i)≈−demean(d_i)` iff recovered). **Mechanism:** SVR needs inter-slice redundancy (overlap) to triangulate; one-slice-per-plane provides none → structural, not tunable. **Implication:** SVRTK-vs-VGGT motion-recovery row = **SVR≈0** by construction; the ".dof motion scorer" TODO is answered (demonstrate non-recovery, don't score accuracy). Gotcha: multi-stack `reconstruct` needs `-thickness` per stack. Repro `scratch/eval/engine/prove_motion_nonrecovery.py` → `motion_nonrecovery.json`; report `_html/svrtk_motion_nonrecovery.html`. |
| [`41_svrtk_motion_bakein_imagespace_and_rtfb.md`](41_svrtk_motion_bakein_imagespace_and_rtfb.md) | **Image-space proof (both axes, 43 subjects) that SVRTK bakes breathing in + what RTFB redundancy would change** (2026-07-13, image-space companion to doc 40). Instead of scoring SVR's `.dof`, measures *where the reconstructed anatomy landed*. Breathing is **through-plane-dominated** (dZ 7.2 mm ≈ 0.6 pitch, 2.07× in-plane dY). Depth-registering each recon slice vs GT: content sits at the breathing-**shifted** depth — **CMRx through-plane slope 0.77 vs injected dZ (control 0.17 planes), in-plane retains ~77 %** (control 0.19 vox); MIITT in-plane clean (0.56), MIITT through-plane under-controlled (flagged). Per-plane: **85 % of moving planes baked in, median correction 5 %, binned ratio ≈ 1.0 at every magnitude ⇒ ~0 % systematic correction**; the 15 % "corrections" are noise (10 % point the wrong way, residual std ≈ control noise). **Reconciles the "but `.dof` has large tz (max 28 mm)" objection:** those transforms are spurious (nonzero on motion-free clean stacks, uncorrelated with truth) → random jitter around the acquired position, never a pull to truth. **2-agent debate on real-world RTFB** (many frames/slice): converged that **through-plane stays structurally limited** (single-orientation SAX = zero through-plane overlap → no depth reference; only lever is respiratory **gating/selection**, `docs/35`, pitch-bounded + SNR-costly), and **in-plane improves only as *deblur* toward the respiratory-**mean**, not the end-expiration GT** — plateaus below the clean-control floor; the real respiratory lever is gating, not SVR registration. One-line: *extra frames sharpen the in-plane image toward the average breathing position but can't fix the dominant through-plane motion; only gating helps — the ceiling VGGT bets a learned prior can beat.* Discriminating experiment specified (cardiac-only resp-varied multi-frame SVRTK; per-axis split vs GT-vs-respiratory-mean) but **not run**. Scripts `scratch/eval/engine/prove_motion_{imagespace,throughplane}.py`, report `_html/svrtk_motion_imagespace.html`, figures `result/svrtk_motion/`. |
| [`43_oneframe_ablation_eval_plan.md`](43_oneframe_ablation_eval_plan.md) | **One-frame ablation: what to evaluate, compared against what** (2026-07-13, plan written before results). The 5-run controlled ablation (`sbatch/oneframe_*.sh`, all warm-started from 4wok, 1-frame, 100 ep, single-delta each) laid out as a **hub-and-spoke**: `gather05` (gather=0.5) is the hub; each treatment is a single-factor A/B against it. **Four questions:** (C1) does the **gather aux loss** help? no-gather vs gather05 → judged on **breathing** metrics (docs/37/38); (C2) does **data aug** help? gather05 vs +aug → judged on **OOD transfer** not in-dist (aug hurts in-dist, docs/05); (C3) does **continuous physical-z** help? gather05 vs +contz → judged on **off-grid OOD** (MIITT 10 mm) with a **motion-not-killed guardrail** (the s20contz snap); (C4) does **unfreezing DINO** help? gather05 vs +dino → **overall PSNR** with a **forgetting guardrail**. Metrics from 3 layers: in-dist wandb val (docs/38 ship-decision `recov_frac_heart`/`psnr_motion`/`hole_frac` + EF sweep), offline OOD head-to-head (docs/42 `run_vggt.py` vs SVRTK/NeSVoR), nnU-Net EF/Dice (docs/39). **Decision rule:** win iff `recov_frac_heart`↑ & `psnr_motion`↑ WITHOUT `hole_frac_heart`↑, plus the factor metric. **Fresh series** — re-eval all 5 under ONE common protocol; not comparable to old gather05/docs/42 numbers. |
| [`42_vggt_model_eval_headtohead.md`](42_vggt_model_eval_headtohead.md) | **VGGT-model eval on the frozen breathing bundles — gather05 head-to-head vs SVRTK/NeSVoR** (2026-07-13). Built `scratch/eval/engine/run_vggt.py` (GPU counterpart of the classical `run_svrtk3d.sh`/`run_nesvor.sh`) that runs a VGGT ckpt on the **frozen breathing** (never re-sampled — the pre-existing `run_cmrxrecon.py` re-samples a *different* realization) and is scored by the **identical** `assemble_and_gif.py`+`aggregate.py`, so the comparison is provably fair. **Result: VGGT loses *clean* but WINS under *breathing* on both datasets, far more breathing-robust — confirmed by PSNR *and* SSIM *and* NCC (not a normalization artifact).** CMRx (30): breath **20.6 vs SVRTK 17.6 dB**, cost **−1.5 vs −10.6**, wins 29/30. MIITT (13): breath **16.2 vs NeSVoR 15.1 vs SVRTK 14.8**, cost **−1.4/−2.9/−6.4**, wins 10/13. **Mechanism:** model predicts breathing-corrective through-plane motion (slope **0.73 in-dist**/0.45 OOD; in-plane far better, EPE 0.4–0.6 mm; deep-breath undershoot ~61% = residual) vs classical slope ~0 (doc 40). SVRTK wins *clean* only because its input is oracle-gated coherent while VGGT's clean is still scattered-phase-blind. **continuous-z ≈ snapped** for the integer-z-trained gather05 (snapped=headline; contz needs a retrain to pay off). **prove-it fixed a reference-plane bug** (canonical-center→content-bbox-center, matched training, 22/30 CMRx affected) + made `resp_diag`/`loss.py:metric_resp_slope_dz` include slot 0 (val-only). Data efficiency ~6–8× fewer frames whole-cycle (~21 vs ~122 CMRx, ~39 vs ~305 MIITT) — but deeper win is the gating-free scattered acquisition; non-ref frames are at random model-unknown cardiac + per-slice respiratory phases. **`--stage-tmp`** cuts model load 266 s→~14 s (GPFS small-read fix; stage ckpt on /tmp). Figures `result/vggt_eval_analysis/`, GIFs `result/*_cmp_*.gif`. Model=gather05 (`216539845_…ftgather05_1frame`, wandb `81li618p`). |
| [`44_oneframe_ablation_results.md`](44_oneframe_ablation_results.md) | **One-frame ablation results — breathing is NOT the bottleneck; the appearance wall is** (2026-07-15, MID-TRAINING: the 6 runs died at ep 25–39 and were resumed, nothing converged; prove-it-audited, and the audit REFUTED two of my own first-pass conclusions). Executes the docs/43 plan. **THE RESULT: the paradox is resolved, and the resolution has a SCOPE that flips ship decisions.** gather05 vs no_gather reconstruct volumes that **agree with EACH OTHER (26.9 dB) far better than either agrees with GT (20.3 dB)** ⇒ **89% of the IN-DIST error is common-mode, 11% unique** — so a real 2.6× placement fix is worth **+0.04 dB (p=0.48, NULL)**; the 5.4 dB of oracle headroom is *appearance* headroom (oracle gets the true target-phase content the model never sees — docs/22/24). **BUT the identical decomposition on REAL OOD (MIITT) gives 71% shared / 29% unique — the placement component is 2.7× larger — and gather's payoff tracks it exactly: +0.402 dB, p=0.020, 10/13.** ⇒ **"gather buys ~0 dB" is IN-DISTRIBUTION ONLY**; reported without that scope it is the wrong ship call. **VERDICTS (paired per-subject, both cohorts, all 12 passes at full n): C1 SHIP** (null in-dist, +0.402 OOD p=0.020); **C2 SHIP** (aug −0.083 in-dist n.s. / **+0.632 OOD p=0.016** — exactly docs/05's predicted trade; aug also has the best breathing of the series AND the lowest within-series OOD relocation); **C4 IN-DIST ONLY** (dino is the ONLY significant in-dist winner, +0.161 p=0.004 22/30, but **flat on OOD** p=0.71 — judging C4 on in-dist PSNR as docs/43 §2 specified would have shipped a non-transferring change); **C3 NO-SHIP in-dist** (worst of six, −0.559) but undecidable there (oracle handicap); **C5 NULL** (its OOD "win" +0.525 12/13 is CONFOUNDED — it is the least-trained model, ep25; note less training ⇒ better OOD, same direction as the relocation shrinking with epochs ⇒ OOD may be non-monotonic in training = in-dist overfitting, a hypothesis for ep100). Best OOD = aug 16.786; best in-dist = dino 20.745; both classical baselines lose under breathing (SVRTK 14.835, NeSVoR 15.118). **⚠️ Never quote MIITT `clean_psnr`/`cost_psnr`** — several models score BETTER from breathing-corrupted than clean input (negative cost), so MIITT's clean arm is not a valid reference; the breath comparisons are sound. **(C1) gather works decisively for PLACEMENT + COVERAGE, and slightly COSTS PSNR** — paired over the plateau (ep20–38, n=19, val data verified byte-identical): EPE 1.37 vs 2.89 mm (t=−16, 19/19), heart coverage holes **0.064 vs 0.091 (−29%, p=1e-3)**, but recov/psnr_motion are **coin-flips** and gather is *significantly worse* on bbox (−0.135 dB, p=9e-4) and static (−0.372 dB, p=2e-6). **(C5) lowdiff100 NULL** (rule-out, reproduces docs/33). **(C3) contz UNDECIDABLE in-dist — my "it smears" reading was REFUTED**: contz's **ORACLE** (Δ=0, true content, zero misplacement, no model) is already **3.04 dB worse** (22.99 vs 26.04 dB) and **constant at every epoch incl. ep1** — a structural handicap *larger than its whole measured deficit*; its inputs are 2-plane z-blends (`mri_dataset.py:434-440`) destroyed before the model sees them; and my "fewer holes" premise was an **epoch-switch error** (identical 0.0552 vs 0.0553 at my own ep26). In-dist val structurally CANNOT measure what contz is for → decide on OOD. **BREATHING (the headline question): good, and better than a first read** — on the **270/300 content-bearing slots** slope **0.844** / corr **0.958** / EPE **1.10 mm**, **0/100 big breaths ignored** on slices with anatomy; the only 8 "misses" are slices breathing **evacuated out of the FOV** (94% empty, heart-free, z_norm 0.90) — ill-posed, and *both* models fail them identically (8/16). no_gather by contrast ignores **23/100 content-bearing** mid-stack slices *with heart*. Null (predict Δz=0) EPE = **5.18 mm** ⇒ hub removes **73%**, no_gather 53%. Clean negative control **0.14 mm** (invents nothing). Under-correction is REAL (Theil–Sen 0.812, heart-only 0.782, dropping ill-posed slots 0.807→0.829 — mixture-artifact hypothesis dead) but **magnitude-dependent**: worst ~55% at 2–5 mm, 81–88% at ≥5 mm. Offline harness cross-validates the trainer metric via an independent path (0.814/1.31 vs wandb 0.852/1.32). **⚠️ ON REAL OOD (MIITT) THE BREATHING METRIC MEASURES RELOCATION, NOT BREATHING.** The clean negative arm (applied ≡ 0) degrades **0.14 mm in-dist → 8.8 mm on MIITT**, strongly **structured in z** (r=−0.73, p=3e-23; +13…+22 mm on planes 1–4, ≈0 on 6–11) = a whole-field relocation on data with NO breathing (reproduces `gather_contz_relocation_not_flicker`). **The trap:** MIITT's *slope* still looks healthy (0.86) because a constant offset barely moves the slope while destroying corr (.958→.715) and EPE (1.03→4.15 mm) — **never quote a MIITT breathing slope without the clean control beside it**. **The obvious cause is REFUTED by measurement:** the adapter's native-10 mm→canonical-12 mm snap (`inference/adapters/base.py:69-73`) is a **sawtooth bounded at ±6 mm**, but the observed Δz peaks at **+21 mm (3.5× the bound)** and is a smooth hump — snap cannot explain it. **Best lead = UNDERTRAINING:** all curves share one z-shape but amplitude tracks maturity (ep39 hub 8.88 / aug 8.25 vs **ep100 docs/42 4.23 — half**) ⇒ re-run `tools/fig_ood_relocation.py` at ep100 to test. Live alternative with the OPPOSITE sign: MIITT gated is a **real** acquisition (slices minutes apart, breath-holds drift), so the clean arm may not be a valid negative control at all and the model may be **correctly** fixing real drift — **so do NOT read this as a failure yet**. **Within-series aug BEATS the hub on every OOD measure** (clean 8.22 vs 8.84, EPE 4.15 vs 4.71, corr .715 vs .614); an earlier "aug doubles the relocation" claim was a **cross-series** (ep39-vs-ep100) error, retracted. **A hypothesis of OURS that FAILED** (don't re-run): splat-quantization predicted gather's gain grows with breath depth — refuted (r=+0.066 p=0.735; deep vs shallow p=0.706; and gather's edge is *larger on CLEAN input* (+0.151 dB) than under breathing (+0.025), so it isn't a breathing effect at all). **Method traps: (1) `Val_Loss/metric_*` keys are DEAD** (real: `val/resp/*`, `val/metric/*`, `val/psnr/*`) and **contz bakes a different identity baseline into its key (`base17.3` vs `17.1`) → string-matching silently drops it**; **(2) `val/ef/slope` at n=29 is NOISE** (hub's own range .08–.63; CI at r≈0.3 spans the whole between-run spread) ⇒ **nnU-Net EF/Dice Layer C NOT run** — but contz's EF *is* genuinely separated (p≈0.02–0.03, 7/8, two metrics), and lowdiff100's "0.000" is **truncated/uninterpretable**, not noise (5.3σ outside its own band; the hub made the same excursion at ep21 and recovered); **(3) never read a metric at one epoch — pair over the plateau** (ep26 mildly flatters gather); **(4) the checkpoint, not `log.txt`, is the authority on its epoch** — the trainer prints the save line *before* writing, so `no_gather` is ep37 and `dino_ft` ep33, one behind their logs (tell = a leftover `.tmp`). Tools `tools/{pull_1frame_series,analyze_1frame_series,analyze_breathing_1frame,plateau_paired_test,compare_recons_1frame,verify_ignored_slots,gather_benefit_vs_depth,zshift_sensitivity,breathing_roi_probe}.py`; report `_html/44_oneframe_ablation_analysis.html`; ckpts `scratch/checkpoints/20260715_1frame_*`. **[prove-it-corrected 2026-07-16: OOD shared-error fraction recomputed via direct MSE (peak-bug fix) 71%→74%; in-dist 90% unchanged; z-shift scope caveat added.]** |
| [`46_oneframe_ablation_ep100_final.md`](46_oneframe_ablation_ep100_final.md) | **One-frame ablation FINAL at ep100 across 4 cohorts (in-dist CMRx + OOD MIITT/OCMR/ACDC)** (2026-07-21). Brought OCMR-gated (8) + ACDC (40, pathology) INTO the frozen head-to-head harness (new `scratch/eval/{ocmr,acdc}/build_inputs.py` + `run_vggt.py` datasets). **Pooled OOD (n=61) resolves docs/45's underpowered calls and FLIPS the headline: (C2) aug is a ROBUST OOD WINNER** (+0.41 dB, p<0.001; MIITT+ACDC both sig) at −0.13 in-dist — docs/45 only mis-called it a non-replication because it had n=13 MIITT alone. **(C4) dino is a mild UNIFORM winner** (+0.19 pooled OOD p=0.042; +0.21 in-dist) — transfers at n=61, revising docs/45's "overfits". **(C1) keep gather** (in-dist+ACDC, neutral pooled; decisive for breathing). **(C3) contz NO-SHIP** (worse both arms, partly structural). **(C5) NULL**. **Appearance wall holds** (≤1.0 dB within-cohort spread; MIITT widest 1.04). Cohort difficulty CMRx 21.0 > ACDC 19.1 > OCMR 18.7 > MIITT 16.0; OOD relocation 4–10 mm (MIITT worst). In-dist EF ~0.4–0.5 (wandb, n=29, power-limited, 1-frame ceiling). **OOD EF/Dice now RUN** (nnU-Net Task114, hub, n=61): absolute EF is NOT usable (under-predicted 19.7–29.2 pts on every cohort) but per-patient ranking holds where EF varies — ACDC slope 0.52/ρ +0.74 (GT sd 19.0) vs MIITT +0.47 / OCMR +0.07 (sd ~6.7, restriction of range, so **OCMR's ρ≈0 is NOT a negative result**); Dice ED LV 0.84/MYO 0.65/RV 0.74 on ACDC. Tools `tools/{paired_verdicts_1frame,fig_ep100_summary,fig_ood_ef,ef_dice_1frame}.py`; figures `result/1frame_ep100/`. |
| [`47_trainstep_profiling_and_torch_compile.md`](47_trainstep_profiling_and_torch_compile.md) | **Train-step profiling & `torch.compile` evaluation** (2026-07-23, prove-it-verified). Full per-step profile of the 1-frame pipeline (`mri_volume_diffusion`, S≈10, aggft, bf16+TF32+gradient-checkpointing): step is **68% backward / 29% forward**, forward is **~87% aggregator attention** (`point_head` 137ms fp32, splat <1%, dataloader ~0 once /tmp monai cache warm). Kernel-level, **~33% is FlashAttention + cuBLAS GEMM that `torch.compile` cannot accelerate**; the rest is fragmented elementwise between checkpoint/attention boundaries. **Measured: `torch.compile` is a NET LOSS — 4–6% SLOWER in every no-DDP mode** (default AND max-autotune, model-only AND full-step) **and FAILS under DDP** (`BackendCompilerFailed`: torch-2.3.1 autocast/fp32-bias bug in the DPT head; graph breaks 4→13) — and the trainer is DDP-wrapped even on 1 GPU (`trainer.py:396`). Compile is numerically faithful (loss rel-err 3.9e-6 on identical weights) — purely slower. **Refutes "full-step compile is probably much faster."** Dominant cost = attention backward + mandatory checkpoint recompute = FlashAttention (compile-immune) + memory-forced recompute; only large lever left is reducing S (research decision, which also blocks compile via variable-S recompilation). Repro: `tools/profile_trainstep_compile.py`; 2 measurement bugs (head bf16-vs-fp32, weight-drift numeric match) found+fixed by prove-it before the numbers. |
| [`48_sfm_stack_and_target_t_removal.md`](48_sfm_stack_and_target_t_removal.md) | **Removed the original-VGGT SfM stack + retired the legacy target_t path — byte-identical cleanup** (2026-07-24). Deleted the inherited multi-view-SfM machinery (camera/depth/track heads + their losses + supervised-DVF `compute_point_loss`) and the deprecated **target_t-index** conditioning (`TIndexEmbedder`/`target_t_embedder`, replaced by reference-slice conditioning in docs/24/25). **Net −2,742 lines** across `training/loss.py` (1122→515), `vggt/models/{vggt,aggregator}.py`, the 3 mri configs (+ deleted `default_dataset.yaml`), `trainer.py`, and tests. Hard constraint: active `mri_volume` stays **byte-identical** — **verified** (fixed-seed `torch.equal` on 138 tensors: loss dicts + aggregator `state_dict` + forward; config-resolve diff = only-dead-keys-removed; full pytest green). Key move: **`**kwargs` on `VGGT.__init__`/`Aggregator.__init__`** so the ~85 files passing retired flags (incl. active `inference/inference.py`) don't break. Branch **`cleanup/remove-sfm-stack`**, base `4b55619`; recover anything via `git show 4b55619:<path>`. 4 target_t-only tools archived to `tools/_legacy/`. **Deferred (status): ALL DONE** — `vggt/dependency/` tree (`d88bd03`), dataset SfM filler keys (docs/50), DDP removal (`284992c`), trainer viz-extraction + TensorBoard removal + dataset base-class collapse (docs/51). |
| [`45_oneframe_ablation_ep60_progression.md`](45_oneframe_ablation_ep60_progression.md) | **One-frame ablation re-run on the resumed ep44–60 checkpoints — which docs/44 findings replicate** (2026-07-16, prove-it-audited). **Mechanistic findings held/sharpened; marginal n=13 OOD ship-wins did not.** (1) OOD relocation shrinks with training (paired ep39→ep60 p=0.015, 8.85→7.61mm) — SUPPORTED, but artifact-vs-real-drift cause still UNTESTED. (2) C1 breathing separation GREW to 2.9× (no_gather breathing DEGRADES with training). (3) appearance wall persists (90% shared error, direct-MSE). (4) EF still noise. (5) ALL THREE marginal ep39 OOD wins (C1 p=.02, C2 p=.016, C5 p=.008) failed at ep60 — C1 direction held but underpowered (CI[-.10,+.65], paired SEM .17), C2 reversed, C5 collapsed to null; none survived multiplicity. (6) C4 dino overfits (in-dist +0.261 p=1e-4 STRENGTHENED, OOD flat p=.75). C3 contz NO-SHIP both cohorts (oracle-handicapped). Meta: robust n=30 in-dist effects replicate, marginal n=13 OOD wins don't. Tools `tools/{fig_reloc_vs_epoch,compare_recons_1frame,...}.py`; report `_html/45_*.html`; ckpts `scratch/checkpoints/20260716_1frame_*`. |
| [`50_dataset_filler_strip_and_ckpt_staging.md`](50_dataset_filler_strip_and_ckpt_staging.md) | **SfM filler-key strip + node-local checkpoint staging** (2026-07-24, both prove-it-clean). **(A)** Stripped the 8 dummy SfM batch keys (`extrinsics`/`intrinsics`/`depths`/`cam_points`/`world_points`/`point_masks`/`geom_masks`/`tracks`) from `MRIDataset.get_data` — nothing on the MRI path read them (the live `world_points`/`cam_points` are the model *output*); deleted the `co3d`/`vkitti`/`track_util` orphans + dead `load_track` path; `batch_size` now off `data["images"]`. Byte-identical (smoke loss `0.0506`); completes docs/48's deferred filler-key item. **(B)** New shared `vggt/utils/checkpoint_stage.py` copies a ckpt to node-local `/tmp` once (dodges GPFS's ~266s seeky `torch.load` → ~5s), keyed by `sha1(abspath)`, validated by `(size, mtime)` so a re-saved source is re-copied never served stale. Training stages only the immutable base weights (never mutable `checkpoint_last.pt`); inference (`inference.py`, all 3 `run_*.py`) stages every load. Placed in `vggt/utils/` (not `train_utils/`) so `run_rtfb` — which lacks `training/` on its path — can import it. Proven on the real 5GB weights (59s copy, matching sha256, 0s cache-hit); 6 unit tests. Commits `badb9e9`/`7d454e0` + this session's staging refactor. |
| [`51_trainer_viz_split_and_dataset_collapse.md`](51_trainer_viz_split_and_dataset_collapse.md) | **Trainer viz split + TensorBoard removal + dataset base-class collapse** (2026-07-24, prove-it-clean, 6 reviewers). Completes docs/48's deferred list. **Net −2901/+27 lines.** (A) **TensorBoard removed** — it received *scalars only*; the visual wrapper `_log_visuals` had **zero callers** (all 12 visual logs go wandb-direct with `wandb.Image`), so TB never got a single image. (B) **1039 lines of viz → `training/trainer_viz.py`** as `TrainerVizMixin`; trainer.py 2227→1163. Safe despite trainer.py's `import *`: symtable/ast/dis found **zero unresolved globals** (all 30 heavy-dep usages already had method-local imports). `_log_tb_visuals` renamed `_log_visuals_to_wandb` — it was always the *wandb* dispatcher. (C) **Dataset collapse** — `process_one_image` is **unreachable** (reads `self.training`, never set on MRIDataset → AttributeError), and was the sole consumer of `dataset_util.py` → `vggt/utils/geometry.py`; all three deleted (~1200 lines) + 3 orphan SfM pose modules. **`inverse_log_transform` KEPT** (activate_head calls it). (D) **`ComposedDataset` deliberately NOT collapsed** — its `inside_random` IS the train sampler and its p=0.0 colour jitter *consumes a `torch.rand(1)` per train sample*, so removing it would shift the RNG stream. **Byte-identical** via control-vs-treatment: 204/204 tensor digests + every RNG fingerprint (incl. next-draws) identical; 211 tests; smoke reproduces loss `0.0506` + baseline `25.45/23.97/17.27`. Fixed pre-existing defects (6 figure leaks incl. the dead `WandbLogger.log_visuals`, `_stash_ed_es`/`_log_visuals_to_wandb` per-batch-vs-per-sample indexing, stale DDP docs); **reverted** an attempted `epoch -= 1` guard — it flips cadence gates when a completed run is restarted. |
| [`52_zero_break_torch_compile_optimization.md`](52_zero_break_torch_compile_optimization.md) | **Zero-break PyTorch 2.13 `torch.compile` optimization campaign** (2026-07-26, complete & empirically verified, 0 graph breaks across full step, 20/20 pytest green). Eliminates 53+ forward graph breaks and 10+ loss graph breaks via: (1) Aggregator Host-Sync fix (`vggt/models/aggregator.py`); (2) RoPE dynamic Python max position derivation + `frequency_cache` HOP side-effect guard (`vggt/layers/rope.py`); (3) Branchless `torch.where` intensity normalization (`vggt/utils/splat.py` + `training/loss.py`); (4) Porting MRI2CT's `@torch.library.custom_op` traceable 3D fused SSIM implementation (`vggt/utils/fused_ssim_compat.py`); (5) Pure 3D spatial tensor vectorization of `anatomy_bbox` and `motion_mask` validation metrics without compiler guards (`training/loss.py`). Empirically verified `Graph break count (Forward + Loss): 0` on A40 GPU. Includes developer guidelines for preserving zero graph breaks. |
| [`53_clinical_downstream_tasks_and_utility.md`](53_clinical_downstream_tasks_and_utility.md) | **Downstream clinical applications, technical scope & feasibility matrix of VGGT-MRI** (2026-07-26, deeply researched & prove-it audited). Documents 12 downstream clinical applications across cardiology, radiology, pediatrics, electrophysiology, and cardiac surgery, categorized into 3 technical feasibility tiers: Tier 1 (6 tasks fully satisfied now: pediatric CHD, heart failure LVEF qualification, ARVC 3D RV, pre-TAVR/TMVR sizing, CRT dyssynchrony, 3D printing/AR), Tier 2 (3 tasks feasible with post-processing calibration or LGE overlay: 3D strain cardiotoxicity, EP scar fusion, HCM stress SAM), and Tier 3 (3 tasks verified as true modality/contract bounds: constrictive pericarditis septal bounce, 4D Flow WSS, stress perfusion contrast wash-in). Includes regulatory FDA 510(k) (Product Code LLZ) clearance pathway, multi-center N=150 non-inferiority clinical trial design, and complete **Empirical Dataset Audit Matrix** with HVSMR 2.0 (`cropped.zip`), M&Ms-2, ACDC, MIITT, and LAScarQS 2022 dataset integration specifications. |
| [`54_espirit_input_domain_fix.md`](54_espirit_input_domain_fix.md) | **The ESPIRiT input-domain bug, its fix, and the full v1→v2 re-reconstruction of all three CMRxRecon years** (2026-07-27). `EspiritCalib` was fed an IMAGE where its API expects K-SPACE, so the coil maps degenerated to ~uniform magnitude and SENSE collapsed into a phase-aligned coil sum that never divided out the receive-coil (B1−) shading — `corr(SENSE, RSS)` 0.984 at the heart falling to **0.651** at the periphery (1.000 → 0.981 once fixed). One-line fix; **851 volumes re-reconstructed** (live cohort **850** = 2023 196 / 2024 **294** / 2025 360; `CMRx24_Train_P002` has 2 slices and was moved back to the archive), with v1 preserved at `CMRxRecon*_recon_v1_espirit_imagedomain/`. Overturns the old "keep the bug for cross-year consistency" decision (severity varied by year). **Z-pitch relabel + full verification now DONE** (§10): 6,370 files relabeled (0 errors), then all 850 subjects / 11,050 files **fully decompressed** → **0 problems** — correct pitch (2023 193@12+3@10, 2024 294@12, 2025 323@12+37@10), LPS everywhere, 0 zero/duplicate frames, every 3d frame bit-identical to its 4d slice, and every subject confirmed to **differ from its v1** (no stale leftovers). Verifier `tools/verify_recon_v2.py` was **fault-injected with 13 broken variants before being trusted** — which caught a `defaultdict` lookup bug that would have silently no-op'd the 2024 pitch check (§10a). **2025 slice pitch MEASURED, not assumed** (§10c): the 114 blank-`SliceThickness` subjects sit at 3 Siemens centres, and since pitch is a per-*centre* protocol property that's 3 unknowns not 114 — measured via LV length on the LAX ÷ count of LV-bearing SAX slices, calibrated on documented-thickness controls (reproduced their known 12.0 mm to **+2.1%**). Result: **`Center006/Prisma` = 10 mm, not 12 → RELABELED** (60 subjects, 780 files, `recon_report.json` updated in step); `Center001/Vida` confirmed 12; **`Center004/Aera` UNRESOLVED (12 vs 14, 44 subjects) and unmeasurable — only 4 of its 44 ship any LAX, all used**. The **+4 mm gap rule HOLDS everywhere**; only *thickness* varies by centre — which **refutes** the earlier "contiguous ~6 mm" hypothesis, and shows Prisma's 180–216 mm stacks are genuine **over-coverage** (LV ≈120 mm of 18 slices), not a geometry error. Blind controls caught two of my own analysis bugs: `L/(N−1)` biases +14.3% (use `L/N`), and a candidate list capped at 12 mm silently snapped Aera to "conclusively 12". Philips `FOVy` still open (nnU-Net LV diameter decides). **Duplicates settled for all 3 years** (§11): 2023 **68** / 2024 **7 pairs** / 2025 **1 pair** — all excluded at the *data* level (never reconstructed, or archived), so splits never see them; live cohort **849** (196+294+359), pooled unique 894. All pairs visually confirmed as one scan released twice (corr 1.000000; 2024/2025 bit-exact, 2023's ~1e-8 residual **measured** to be the A40-vs-L40S float32 floor, ~8e-6 relative, via a same-subject control). The 2025 scan had a silent key bug that dropped 4 TaskR1 TestSet subjects from the comparison (401 keys from 405 files) — fixed + asserted (§11b). Also documents: the silent **all-zero-slice** failure mode (real, fired twice, **guard now implemented + tested**, §8), the sm_75+ GPU requirement (V100 cannot run a kernel yet `torch.cuda.is_available()` returns True), the 377 GB duplicated-k-space cleanup, and the **downstream regeneration still owed** — `heart_seg`/`heart_roi`/DVFs and the ~270 GB `scratch/eval` harness plus every `evaluation/results/` number. |


| [`55_philips_readout_oversampling_spacing.md`](55_philips_readout_oversampling_spacing.md) | **The readout-oversampling spacing rule, and the Philips `pixel_x` fix** (2026-07-27). `reconstruct_subject` derives in-plane spacing as `FOVx / ReconMatrix_X` — the pixel size only if `ReconMatrix_X` counts **acquired readout samples**, which it doesn't (it's an **output grid size**). Every Siemens subject escapes because `nx = 2·rx` with `ReadOutOversample = 2` makes `base = nx/2 = rx`; **2025 Philips acquires `nx = 304` onto `rx = 256`, so `base = 152 ≠ 256` and the stamped 1.168 mm under-scales the readout axis by `256/152 = 1.684`.** **Fix applied**: `tools/fix_philips_pixel_x.py --apply` → `pixel_x` **1.168 → 1.967 mm**, 12 subjects / 156 files, header-only, atomic, reversible (`--revert`); **`pixel_y` untouched — it was already right**, because ky zero-filling preserves FOV while readout cropping preserves pixel size. Result: anisotropic 1.967 × 1.168 grid whose *acquired* voxel is isotropic at 1.967 — the same pattern UIH volumes already carry. **Supersedes the 2025 README's "stretched along the phase axis, relabel `pixel_y` → 0.689" diagnosis**, which had the aspect right (~1.69) but the axis and direction wrong (its fix implied a ~180 mm thorax). **Philips is no longer excluded** from the (not-yet-built) 2025 splits — an exclusion that was never encoded anywhere but the prose. Also establishes that the FOV convention is **not a vendor property**: Siemens **Sola/Avanto** quote `FOVx` oversampled exactly like UIH, so the driver's `startswith("UIH")` branch is only accidentally harmless (`nx = rx`); **and no metadata-only replacement was found** — the proposed √2 `FOVx/FOVy` test flips **14 subjects by a factor of 2** (genuine rectangular FOVs, `FOVx/FOVy` 1.44–1.73; the existing vendor branch gets all 14 right, so it would add 14 errors to fix 12, §4a), and `nx/rx` does not separate the conventions either (Philips *nominal* at 1.19 vs UIH *oversampled* at 1.33). The vendor branch remains an **empirical lookup**. §4b states precisely what is derived (that `299/256` is wrong under *either* convention, since `ReconMatrix_X` is an output grid size and the recon crops rather than resamples) versus inferred (that the replacement is 1.967 and not `299/304 = 0.983` — separated only by anatomy, since 0.983 implies a ~150 mm thorax). ⚠️ **Confirmation still owed** — evidence is arithmetic + shared-mm-scale visual only: **three hand-rolled LV detectors failed their own controls during this work and every LV-diameter number from it is withdrawn**; nnU-Net Task114 is the outstanding check. |

| [`56_cmrxrecon_odd_z_slice_roll.md`](56_cmrxrecon_odd_z_slice_roll.md) | **Every CMRxRecon challenge release rolls ODD-Z SAX stacks by −1 slice** (2026-07-27). In 2023/2024/2025 any stack with an odd slice count stores its **most basal slice LAST, after the apex**; even-Z stacks are fine. Deterministic: **464/466 odd-Z rolled, 0/383 even-Z** — the exact signature of an **`fftshift` where `ifftshift` was needed on the slice axis** (differ by −1 for odd N, identical for even N). **In the shipped k-space, not our recon** (plain RSS from `cine_sax.mat` reproduces it; constant across all 12 phases), and **absent from the CMRxRecon-300 paper release of the same 300 volunteers** (0/129 odd-Z) ⇒ the challenge repackaging introduced it. Controls ACDC 0/54, M&Ms-2 1/122 odd-Z. Two independent estimators (local cyclic-adjacent argmin + global all-pairs monotonicity) **agree 849/849**, large margins, vendor/centre have zero explanatory power once Z parity is accounted for. **Impact: 464/849 live subjects (54.7%), incl. 183/294 (62%) of the CMRxRecon2024 training cohort, carry one slice ~10–14 cm from where it belongs**, in `V_gt` and the input slices alike. **FIX APPLIED 2026-07-27** — `np.roll(+1,axis=2)` on the **464 detected** subjects (2023 94 / 2024 183 / 2025 187), **6032 SAX NIfTIs / 21.8 GB rewritten in place**, affine untouched (origin is (0,0,0), no absolute position is shipped), 3d+4d rolled together, atomic, <10 min; re-detection after reports **0 rolled at both parities** and the estimators still agree 849/849. Detected **per subject, not by parity** — which protected `CMRx25_train_Center007_Prisma_P001` (odd Z but genuinely correct, user-confirmed visually) and skipped `CMRx23_Train_P073` — now **EXCLUDED from the cohort**: every one of its 13 slices is at a *different in-plane rotation* (mostly negative adjacent corr, no definable roll, not interleaved, same defect in CMRx-300 ⇒ source data). **ARCHIVED** to `_archive/CMRx23_Train_P073_slice_rotation_excluded/` ⇒ 2023 `Cine_combined/` now **195**, live 3-year cohort **848** (the 849 counts here are the measurement as run). Reversible via `--revert`; 849-row audit in each `_provenance/slice_roll_fix.json` + `result/slice_roll_check/slice_roll_audit_applied.csv`. ⚠️ clear `/tmp/vggt-mri_$USER_monai_cache/` on any node with a warm cache (PersistentDataset keys on paths, not contents); every pre-fix number (`evaluation/results/`, `scratch/eval`, the v2 verification JSONs) is now a different series. Supersedes the 2023 README's "harmless packaging artifact" reading of the same roll. One unexplained exception (`CMRx25_train_Center007_Prisma_P001`); `P073` is separately incoherent in *both* releases; **LAX not tested** (3 views = odd). **Mechanism = an unpaired `fftshift` on a non-transformed axis** (`fftshift` where `ifftshift` was needed, or applied twice — the data cannot separate them): parity-dependence rules out index-off-by-one (would hit even Z), de-interleave (would scramble) and flip (would reverse). Verified prediction: among the axes the recon does NOT transform, `nslice` is the **only** one that is ever odd (`nframe` 0/840, `ncoil` 0/840, `nslice` 465/840) — and the **LAX view axis (length 3, always odd) is rolled by −1 in 7/7 subjects** vs CMRx-300, so the challenge LAX views are NOT in the documented `3ch,2ch,4ch` order ⚠️ both slice-pitch measurements read LAX. Recon code cannot cause it (`for slc in range(nslice)`, `sp.ifft(axes=[-2,-1])`, `all_recons[frame, slc]` — no slice-axis op); an independent RSS from the `.mat` reproduces the argmin; an attempted FFT-free k-space test was **underpowered and is not evidence** (envelope near slice-invariant, spread 0.05 vs 0.47). Detector `tools/probe_slice_roll.py`, figures `result/slice_roll_check/`. |

| [`57_corseg_segmentation_evaluation.md`](57_corseg_segmentation_evaluation.md) | **CorSeg-CineSAX (MedNeXt-L) evaluated as a replacement for our M&Ms nnU-Net segmenter — evaluated, NOT adopted** (2026-07-30). Runs in **`svr` with zero installs** (monai 1.6.0 already ships `create_mednext`; only PyQt6/pydicom missing, both GUI-only) — verified by loading the weights `strict=True` and running a forward pass. On **ACDC (only human-GT cohort here, zero-shot for both models, 100 cases)** the answer **splits by metric**: on boundary **Dice** it is slightly worse than what we run (**0.889** vs nnU-Net-2d **0.896** vs ensemble **0.902**, gap entirely **myocardium** 0.844 vs 0.868), but on **volumes/EF — what `ef_dice.py` actually reports — it is clearly BETTER**: **EF MAE 2.51 vs 4.67 pp** (paired Wilcoxon p=3e-4, wins 35/50 subjects, r 0.987 vs 0.962), LV volume MAE **4.4 vs 8.9 mL**, near-zero volume bias on all 3 structures vs nnU-Net's significant −3.4%/+2.8%/−4.5%. No paradox — CorSeg's errors are boundary-localised and *unbiased*, nnU-Net's smaller but *systematic*; Dice punishes the first, volume/EF the second (an earlier visual read of mine, "CorSeg's myo ring is systematically thinner", is **retracted**: myo volume bias is +0.6%, n.s.). ⇒ **keep nnU-Net for Dice, use CorSeg for EF/volumes** (or both, divergence = QA flag); no wholesale switch, since that re-baselines all of `evaluation/results/`. Also a cheap **second opinion** — 1 in-env model vs a 5/10-model cross-env `nnunet` hop (**measured 8.5× cheaper per volume**: marginal 0.165 s vs 1.40 s/volume, 15.5 s vs 50.9 s wall on a 12-phase subject), and trained on **all ~25 cardiac phases** vs Task114's ED/ES-only, which makes it the better candidate for our 10 mid-cycle target phases (**plausible but NOT measured** — no cohort here has mid-cycle human GT). **Two traps.** (1) The **shipped GUI contradicts the paper**: it resizes slices to 224² *ignoring voxel spacing* (training resamples to **1.25 mm** then center-crops/pads), and `load_image` reduces any volume to **one** slice so it can never segment a stack; our runner implements the paper path, and the GUI path costs ~0.02 agreement Dice on the canonical grid and visibly drops apical RV. (2) **CorSeg collapses on heart-ROI crops 0.889 → 0.413 while nnU-Net is untouched (0.896 → 0.890)** — so on ROI-cropped input nnU-Net wins overwhelmingly; cause **measured**: fixed 224² canvas, full FOV fills 99.7% of it vs 17% for crops, Dice ∝ occupancy (r=0.625, p=3.7e-12), nnU-Net escapes by being patch-based sliding-window. **A predicted fix was refuted**: magnifying the crop to fill the canvas made it *worse* (0.296) by breaking the 1.25 mm scale prior ⇒ no preprocessing trick, the fix is to not crop. **NOT moot in practice — an earlier claim here that "our baselines are not ROI-cropped" is RETRACTED**: I had checked the assembled `cine_clean.nii.gz` rather than `recon_clean/vol_tXX.nii.gz`, which is what `ef_dice.py dump` actually feeds the segmenter. `gt`/VGGT arms are the full canonical `(256,256,12)` cube, but **SVRTK3D and NeSVoR emit small ~1.4 mm isotropic heart-ROI volumes** ((72,87,86), (91,106,98)) — the failing regime. Measured on the real SVR outputs (30 subjects): CorSeg leaves **8/30 NeSVoR volumes blank** (median 269 voxels) and on SVRTK segments only a mid-ventricular band, missing base and apex ⇒ **if CorSeg is used for EF, restrict it to the canonical-cube arms and keep nnU-Net for the SVR baselines**. Also: on ROI crops CorSeg fails **silently** — 23/95 ACDC cases return a completely empty mask (all ES), no exception, valid all-zero NIfTI, where nnU-Net scores 0.75–0.83. Harness verified before use: scorer reproduces `eval_acdc.py` exactly (0.896 / 0.9018 = docs/15's 0.902), `center_pad_crop` matches MONAI `ResizeWithPadOrCrop` on 5 shape cases, and fault-injection (wrong label convention) collapses LV/MYO while sparing RV. Caught **two of my own bugs**, both the same root cause: ACDC's affine 3×3 is `diag(-1,-1,1)` with true spacing only in `pixdim`, so copying `im.affine` into a fresh `Nifti1Image` stamps 1.0 mm — which faked a 0.061 ROI score, and separately made every CorSeg-derived physical volume read ~−95% until the writer was fixed to propagate the input header (Dice unaffected, re-verified identical at 0.8892). Tell: **a uniform error across all structures is a geometry/metadata bug; a structure-specific one is a model or label-convention issue.** ⚠️ **Every CMRxRecon-derived number/figure here is on the DEPRECATED `scratch/eval` bundle** (v1 recon, pre-roll-fix; user-confirmed pending rebuild, docs/54/56) — that includes the val panels AND the SVR heart-ROI counts, all of which must be re-measured after regeneration; the **ACDC** results (all the accuracy numbers) and the *geometric* finding that the SVR arms emit small isotropic heart-ROI grids are unaffected. Tools `tools/corseg/`, figures `result/corseg/`, weights `scratch/data/corseg/`, durable bench data `scratch/data/corseg/bench/`. |

| [`58_multidataset_pooling_and_native_z_grid.md`](58_multidataset_pooling_and_native_z_grid.md) | **Pooling 294 → ~1343 subjects forces the canonical grid off its fixed 12 mm z spacing — designed 2026-07-30, IMPLEMENTED + VERIFIED 2026-07-31** (native-z training-path code, §12; manifest CSV + pooled split file also now built + verified — 940/134/269 ≈ 70:10:20, see §13 — **and the config now points at it — §14**). Adding CMRxRecon **2023 (195) + 2025 (359)** to 2024 (294) plus **ACDC (150)** and **M&Ms-1 (345)** gives ~1343 subjects (proposed split ~1006/100/237, **superseded by the built 940/134/269, §13**; CMRx random-stratified, ACDC carves 15 val from `training/` and keeps the published 50-patient `testing/` intact, M&Ms uses its official three-way split because **Canon appears only in its Validation and Testing sets** ⇒ a free unseen-vendor test). The blocker is geometric: `TARGET_SPACING=(1.4,1.4,12.0)` / `TARGET_SHAPE=(256,256,12)` only ever worked because **CMRxRecon2024 is uniformly 12 mm**, and **~45% of the pooled cohort is not** (pool is ~55% @12 mm, ~36% @10 mm, ~7% @8–9.6 mm, ~2% @5–7 mm; z extents 40–180 mm, so ~5% would also be silently center-cropped by the 144 mm cube). **The natural middle road is a measured trap**: an *identity* splat (Δ=0 — a ceiling, not a baseline) of a 10 mm ACDC stack into the fixed 12 mm target tops out at **25.41 dB** vs **numerically exact (120 dB)** on-grid, because `V_gt` is built by *pull* interpolation (`Spacingd`) while the splat *pushes* — the two agree **only** where input positions coincide with output planes. The same probe condemns **`continuous_z`**, which puts inputs off-grid deliberately: **28.31 dB** at the default `z_jitter=0.5` (31.79 at ±0.25) ⇒ turn it **off** (caveat: that is the identity floor, not the model's ceiling — the model could learn a Δz that re-snaps content, which would look like the whole-field Δz relocation previously seen on `s20contz`; **that link is a hypothesis, unverified**). **Decision: never resample z.** In-plane still → 1.4 mm / 256×256 via `Spacingd(pixdim=(1.4,1.4,0.0))` (MONAI keeps non-positive axes native — confirmed, observed dz 10.000/12.000) + `ResizeWithPadOrCropd(spatial_size=(256,256,-1))`; `V_gt` becomes **literally the acquired slice stack** `(D,256,256)` with `D` = that subject's slice count, `V_canon` allocated to match per sample (batch_size is already 1: `floor(20/20)`), identity splat **verified exact at both 12 mm and 10 mm**. Because stack heights then span ~**30–168 mm**, the grid-relative `z_norm = k/(D−1)*2−1` would make the mm-per-unit ruler vary **5.6×** ⇒ depth must go **physical**: `z_norm = z_mm / Z_HALF_MM`, **`Z_HALF_MM = 90`** (not 72: `ZIndexEmbedder`'s sinusoids have **period 2**, and a 168 mm stack at 72 spans 2.33 ⇒ two planes *of the same subject* get bit-identical sinusoidal features, measured 8.9e-16; at 90 the range is 1.87 and aliasing is impossible). **The subtle consequence**: `splat_to_volume` (`splat.py:33-35`) and `sample_volume` (`splat.py:143`, `grid_sample(align_corners=True)`) both *assume the volume spans ±1* — true today by construction, false with a physical `z_norm`, so a 10-slice/10 mm subject (spanning ±0.5) would be **compressed into the middle half of its own grid** (plane 0 → pz 2.25 instead of 0; error 50% for a 90 mm stack, ~7% for a 168 mm one). Fix `pz = pos_z * z_scale + (D−1)*0.5` with `z_scale = Z_HALF_MM/dz` as a **required** arg — `sample_volume` feeds the oracle behind **`metric_recov_frac_heart`** (docs/38), so a silent default would corrupt the ship-decision metric. Untouched: the network never sees `D` (only model-side `grid_shape` use is `vggt.py:113`, inside the off-by-default refiner), the head is unbounded (`point_activation="linear"`), GPU aug freezes the D axis, and **cardiac phase is not a model input** (`aggregator.py:220` drops `t_indices`) — which dissolves the docs/17 / docs/39 cross-cohort `target_t` hazard for training. Keep **T=12 ED-anchored nearest native frame** (mechanical, not semantic: `phases (T,D,H,W)` rides every batch and the monai transform has a fixed 12-key list; the escape hatch if native T is ever wanted is loading the 4D file with T as the channel dim, which is *simpler* than today's `ConcatItemsd`). Other finds: **`MRIDataset.len_train = 1000`** silently dropped every train subject past index 999 (~1006 proposed) — **FIXED, now `max(1000, len(subjects))`**; **`trainer_viz.py:250`** uses the plane count as the phase count (correct today only because both are 12); **M&Ms stores true-oblique affines** (sampled slice-normal `\|S\|` ≈ **0.437** vs exactly 1.0 for CMRx/ACDC) so `Orientationd(LPS)` scrambles it ⇒ the converter must re-frame the affine into acquisition space and assert `\|slice_dir·S\|>0.9`; **`heart_roi_canonical.nii.gz`** is loaded with no shape check and is on the old 12-plane grid; `cardiac_phase.csv` is CMRx-only and v1-era. Surface beyond the 9 training-path edits: `inference/{adapters/base,run_cmrxrecon,run_gated_ood}.py`, `evaluation/engine/*` (⚠️ confirm with user before editing), 8 test files, ~20 `tools/` scripts. Gate before any long run: Δ=0 identity splat exact over ~30 pooled subjects, **fault-injected first**. Unmeasured: whether this trains better than uniform 12 mm — only that it has no built-in ceiling. Provenance `tools/probe_zgrid_alignment.py` (E1–E4). **[IMPLEMENTED 2026-07-31, §12:** gate built as `tools/gate_native_z_identity.py` (real subjects, fault-injected first) — 35/35 real subjects PASS at exactly 120 dB, fault-injected run 35/35 correctly FAIL at 14–22 dB; `pytest tests/` 203/203 (post-refiner-removal); a real `torchrun` smoke run completed cleanly; 6-reviewer `/prove-it` pass. **Two bugs found and fixed that this doc didn't anticipate:** (1) `training/data/composed_dataset.py::ComposedDataset.__getitem__` is a hand-written per-key allowlist, not a generic collate — it silently dropped the new `dz_mm`/`z_scale` keys, invisible to every unit test (they build batches by hand), only caught by actually running `torchrun`; (2) a **pre-existing, unrelated** `splat_to_volume` boundary bug — the in-bounds check excluded points sitting exactly on the last valid voxel index — harmless under the old grid (top z-plane was usually padding) but would have failed the gate outright under native-z (no more z-padding); fixed to test the continuous position instead of the floored index. **Also: the volume-refiner subsystem (`vggt/models/refiner.py`) was deleted entirely** — native-z made its `grid_shape` sourcing require ground truth (breaking GT-free OOD inference if ever enabled), and since it was unused (`enable_refiner` false everywhere, no trained checkpoint), the user chose deletion over a redesign. `inference/`/`evaluation/`/~50 `tools/*.py` remain deliberately un-migrated (per the standing scoping rules) — they now fail loudly (TypeError/AttributeError), not silently.] **[SLICE ORDER STANDARDISED ON DISK 2026-07-31, §10a/§10b:** sources disagreed about which anatomical end is stored first — CMRx2023/24 + ACDC base-first, M&Ms apex-first, and **CMRx2025-UIH ~50/50 *within one scanner*** (so no per-source rule works). This matters because `respiratory.py` applies a one-sided breathing shift along array axis D with **no anatomical anchor**, so storage order alone decides whether the simulated heart moves inferiorly (physiological) or superiorly — it was **backwards for 66% of the pool**. Detector = taper of the ED segmentation, rule `sign(f1)==sign(f2)` selected **by measurement** (a magnitude-weighted vote and a robust-scaled mean both measured WORSE, 6 errors vs 1; `f3` cavity-fraction is **anti-informative on hard cases, 3/22**; max-|z| tie-breaking 59%). Validated against M&Ms' real geometry — but only after fixing the converter's criterion: it flips on the **S component alone**, whereas apex→base is right+posterior+superior (empirically `R+0.611, A−0.677, S+0.410`); projecting onto that axis gives **343/343 agreement** and resolves `MNMs_T2Z1Z9` (S=+0.087 outvoted 10:1 by L/A ⇒ genuinely base-first, a converter mistake not an anomalous scan). **893/1343 subjects flipped** (`np.flip(axis=2)`, 15030 files, 6 file types incl. both `*_canonical`) by `tools/fix_slice_order.py`, per-subject from `result/slice_order_check/slice_order_decisions.csv`. **Affines deliberately untouched** — all sources already declare `+z = Superior`, so flipping the *array* is what makes the header honest; editing the affine instead would make `Orientationd(axcodes="LPS")` silently flip everything back. Note "LPS" here is a **stamped convention, not measured geometry**: a real SAX stack is double-oblique (M&Ms |S| median **0.402**), CMRx ships no orientation at all and ACDC has `sform=qform=0`. `convert_meta.json`'s `reframe.flips[2]` toggled for the 151 ACDC/M&Ms subjects so `verify_sax_conversion.py`'s bit-exactness proof stays valid (re-verified 150/150 + 345/345 after). **No re-segmentation needed** — a z-flip preserves image↔seg correspondence exactly (the earlier ~15 GPU-hour estimate was wrong). Verified: preflight 893 ok/0 reversed, flip proven an exact involution, `--verify` **fails before** (885 base-first) and **passes after** (1331 apex / **0 base**), fault-inject fires, respiratory direction now 4/4 INFERIOR, `gate_native_z_identity` 35/35 @120 dB. ⚠️ `cache_signature()` bumped `bab7860607`→`f937056bb3` — monai keys on **paths not contents**, so without it a warm node silently trains on pre-flip volumes. Reversible via `scratch/data/_provenance/slice_order_fix.json`; **undo before docs/56's slice-roll fix**.] **[SLICE-ORDER FIX APPLIED + `/prove-it` AUDITED 2026-07-31, §10b:** 893/1343 subjects flipped on disk to apex-at-z0 (15030 files, 6 types, axis 2), affines untouched, `convert_meta.reframe.flips[2]` toggled for the 151 ACDC/M&Ms so `verify_sax_conversion` stays valid (re-verified 150/150 + 345/345 AFTER). Detector validated **320/320 against M&Ms' shipped GT masks** (`*_sa_gt.nii.gz`) — which also proved the "converter guarantees apex-first" premise UNSOUND (it fails at small |S|; `MNMs_T2Z1Z9` is a genuine base-first stack, correctly flipped). Verified: round-trip bit-exact for all 3 dtypes, CMRx 3D-vs-4D 60/60, end-to-end corr(canon seg, native seg) **+1.0000**, identity gate 35/35 @120dB, pytest 203. ⚠️ `cache_signature()` bumped `bab7860607`→`f937056bb3` (monai keys on PATHS not contents). Both fix tools now REFUSE to re-run (a second `--apply` would have silently undone everything; flip and roll do not commute — wrong revert order = silent `roll(+2)` on 410 subjects). Respiratory magnitude also now **measured**: the old hardcoded 12mm held the VOXEL shift constant so the PHYSICAL shift scaled with pitch (−17%@10mm … −58%@5mm); per-subject `dz` makes the mm constant instead. **Still open:** eval bundle rebuild, `baselines/export_resp_stack.py:101` (no `spacing`), 851 unflipped `*_recon_v1_espirit_imagedomain` subjects, and `fix_slice_roll.py::subject_files()` missing the seg/ROI files.] |

| [`59_native_z_pooled_migration_audit.md`](59_native_z_pooled_migration_audit.md) | **7-reviewer `/prove-it` audit of everything docs/58 changed in the training path** (2026-07-31; native-z grid, physical `z_norm`, pooled 1343-subject cohort, refiner removal). **The migration's core is verified correct** — the `z_norm → pz` inverse round-trips to `2e-15`, the 518↔256 x/y map is exact to `3e-15`, breathing is applied in true millimetres at every pitch (fault-injection confirmed), all 1343 subjects load with all 12 frames, `0/1343` exceed `Z_HALF_MM=90` (max half-span **85.00 mm**, 5.9% headroom), `pytest` 215 green, and a real `torchrun` on the pooled config completes end-to-end with **0 tracebacks and 0 swallowed exceptions**. **Three things need fixing first.** (1) 🔴 **A float32 cancellation drops the ENTIRE apex plane for 56/1343 subjects** (45 train / 2 val / 9 test, all M&Ms at `dz` 9.6 and 8.0): `pz` for plane 0 lands at `−4.77e-07` and `splat_to_volume`'s hard `pz >= 0` gate discards the whole slice, not a fractional weight. Measured cost **−9.56 dB / −5.01 dB** identity PSNR on the two affected *val* subjects, with the fix bit-identical (+0.00) on controls. It deterministically corrupts the Δ=0 identity **and oracle** splats behind **`metric_recov_frac_heart`** (the docs/38 ship-decision metric) plus `baseline_identity.json`. A **native-z regression** — the old grid-relative formula gave `pz = 0` exactly. ~4 lines to fix (widen the bounds test by a sub-voxel EPS **and clamp before flooring**; ⚠️ a naive `+eps` nudge is NOT a fix — measured, it trades the apex plane for the basal one, −4.16 dB on a control). (2) 🔴 **`tools/gate_native_z_identity.py` is structurally blind to it** — it masks PSNR on `coverage > 0.5`, so a fully-dropped plane is excluded by construction; it reports **120.00 dB on `MNMs_G7S6V0`, whose plane-0 coverage is 0.0**. Generalizable lesson: *a gate that masks on the quantity a bug destroys can never see that bug* — the fault-injection rule was followed, but the injected fault came from the family the mask tolerates. (3) 🟠 **Three pairs of byte-identical duplicate subjects straddle split boundaries** (train↔test `ACDC_patient055`≡`118`; train↔val `MNMs_A7G0P5`≡`K3R0Y7`; val↔test `MNMs_C8J7L5`≡`C8O0P2`, 12/12 frames each, plus 3 within-train pairs) — **inherited from ACDC and M&Ms across their own official splits** (raw *voxel arrays* bit-identical, only gzip containers differ ⇒ our converter is innocent), and their metadata self-contradicts (identical pixels labelled Philips/centre-2 **and** GE/centre-4), which undercuts the "Canon only in val/test ⇒ free unseen-vendor test" premise. Includes a **priority × effort triage table (F1–F20)**: also **ACDC oversampled 1.60×/epoch** (`1000 % 940 = 60` residual meeting an alphabetically-sorted split file — and the docs/58 §4 `max(1000,…)` fix is *inert* at n=940), a **monai cache that is 66% dead weight** (`ConcatItemsd` never drops the 12 float32 `phase_NN` keys → 47.8 MB/entry of which 31.4 MB is never read, ~54 GB `/tmp` and 3× per-epoch I/O, one-line fix), **val covering only 6 of 12 pitches with ZERO 8 mm subjects** (93% is 10/12 mm ⇒ the generalization claim motivating native-z is largely unmeasurable on val), `z_scale`/`dz` read from element `[0]` with no `B==1` assert (every train `D` carries multiple pitches ⇒ silent 20% geometry error if `max_img_per_gpu` ever changes), **no test that can distinguish the respiratory `dz` fix from its reversal** (every fixture is `dz=12.0` ⇒ bit-identical to the old hardcoded default), `metric_ssim_3d_full` now `D`-dependent (measured 0.9727@D=5 vs 0.9581@D=21 ⇒ not comparable across subjects), `ef_val_sweep`'s ED/ES are **segmentation-derived not GT** (20/49 disagree with source labels, worst half a cycle), and the pre-existing `0.0 * NaN = NaN` splat gate. **Refuted:** `ef_eval.py`'s hardcoded 12 mm does *not* corrupt EF (it's a ratio, and nnU-Net's `PreprocessorFor2D` provably never resamples z). Closes with a **"verified CLEAN — do not re-audit"** register and an explicit list of the four gaps that were *not* verified. **UPDATE 2026-07-31 (two fix passes): 14 of 20 findings CLOSED and verified** — F1 (apex drop; fault-injected 9.96 → 119.92 dB, controls bit-identical, `fullgraph` torch.compile clean with 0 graph breaks), F2 (gate now FAILs at 31.23 dB on the subject it used to pass at a fake 120.00), F3 (6 source-shipped duplicates excluded → cohort **1337**, split **935/133/269**, applied AFTER assignment so the diff is 6 pure deletions with zero subjects moved; `--check-duplicates` guard added and confirmed to find exactly those 6 across all 1343), F5 (cache 47.8 → 18.0 MB/subject, output bit-identical), F6 (`Counter({1: 935})`, ACDC 1.60× oversample gone), F7 (guard on pitch *uniformity*, not `numel==1` — the audit's suggested assert broke 14 legitimate B=2 tests), F4 (**36 new tests in `tests/test_respiratory_native_z.py`**; fault injection proves the point — with the fix reverted the pre-existing 215 tests ALL PASS while the new ones fail), F9+F19 (batch size now **pinned to 1** — measured: different-`D` pairs crash loudly but same-`D`-different-pitch pairs collate silently, so B=1 is the only safe configuration; `max_img_per_gpu` documented inert), F11 (`metric_ssim_3d_full` **replaced** by per-slice `metric_ssim_2d_full`; ⚠️ re-measurement **corrects the audit** — direction is opposite and magnitude ~15× smaller than reported), F17, F18, F20. **F10/F12/F13 reviewed and deliberately NOT fixed** (keep seg-derived ED/ES for cross-source preprocessing consistency; no refiner ckpt will be loaded; val volumes are overwritten in place so the 925 MB is constant, not cumulative). **Third pass also closed F15** (needed TWO changes, not the suggested one — `torch.where` on the gate AND on the positions, since `clamp` propagates NaN; fault-injected 16 NaN voxels + `V.mean()=nan` → 0, still 0 graph breaks; ⚠️ correction: only **NaN** poisons, `±Inf` was already handled by the clamp) and **resolved F16's dispute by measuring it** — 2.18% fully blank / 9.48% partially dimmed / 11.66% either over all 935 train subjects, i.e. the two reviewers (2.74% vs 12.4%) were counting blank-only vs blank+dimmed and **both were right**. Also removed `max_img_per_gpu` outright and renamed `dataset_name` → `pooled1337`. **ALL 20 FINDINGS NOW CLOSED — 14 fixed in code, 6 accepted/wontfix after measurement.** **F8 CLOSED as accepted** (user: don't deviate from the M&Ms official split; and the audit's "unmeasurable" framing was too strong — val already spans **5.0/7.0/8.8/9.6 mm, 9 of 133 subjects**, so cross-pitch is measurable, just under-powered; re-drawing the CMRx splits could never have helped since they are exclusively 10/12 mm). **F16 CLOSED as accepted** on proportionality: measured 2.18% fully blank / 9.48% dimmed, but fully-black slices are **already free** (gated out of numerator AND denominator) and the dimmed remainder is a bounded ~4%-of-slots noise floor — inside docs/46's appearance wall, against a four-part fix. A 3-agent debate identified the option to use if ever revisited (**renormalize by the exactly-known per-slot attenuation + soft splat weight** — verified unbiased at every overshoot, with exclude/keep as its endpoints), and retracted the audit's claim that skipping all-zero slots biases `metric_resp_*` optimistic (a blank slice has no signal to correct). Details + the accepted-limitations list in §9. |

| [`60_on_disk_run_logs_and_val_stratification.md`](60_on_disk_run_logs_and_val_stratification.md) | **Every run now writes its numbers to disk, so analysing a finished run no longer needs wandb** (2026-08-01, implemented + verified). Three append-only files land in `log_dir` — `run_meta.jsonl` (one line per PROCESS LAUNCH: git sha + dirty, resolved config, split/manifest md5, cohort sizes, wandb id, SLURM job/node, `resumed_from_epoch`), `metrics.jsonl` (every scalar), `val_per_subject.csv` (one row per val sample: subject, source, `D`, `dz`, `t_target` + every `metric_*`). Read with `tools/load_run.py`. wandb is unchanged — this is a **mirror**, and the 8 image panels (filmstrip GIF, ED/ES, DVF, …) stay **wandb-only**. Motivation: a finished run's only structured numeric artifact was an **822-byte** `baseline_identity.json`; everything else was a **22 MB `log.txt` of prose** or an **875 MB binary `wandb/`**. Cheap because `Trainer._log_scalar` is the **single scalar chokepoint** (the other 8 `wandb_writer.log` calls all pass images), so a 3-line mirror there catches everything, present and future. Resume is the subtle part: `steps` is checkpointed and restored at **epoch boundaries**, so a requeue **replays** every step since the last checkpoint — the writer keeps both rows (they are the evidence a requeue happened) and `load_run.py` dedupes on `(name, step)` keep-last; JSONL is chosen so a kill mid-`write()` costs **one** row not the file, and **the next append repairs a missing trailing newline** (without it the partial line and the next one are BOTH corrupted — caught by a test, not by inspection). On resume the CSV **adopts the on-disk header** so a differing metric set can't silently shift every column. **Also fixes/extends val logging for the pooled cohort.** Two visualization bugs: every visual panel was pinned to val idx `(0,7,14,21)`, which under the sorted pooled split is **3 ACDC + 1 CMRx2023 — zero M&Ms** (largest val source, n=33), zero CMRx2024, zero CMRx2025; and the filmstrip rendered `D//2 ± 2`, which is **provably the reference slot** (`z_mid = (bbox_z0+bbox_z1)//2`, and native-z never pads z ⇒ bbox `[0,D)` ⇒ `z_mid == D//2` exactly) covering 83%→28% of the stack as `D` varies 5–21, and at `D=6` **never rendering apex plane 0 — the docs/59 F1 plane**. Both fixed (one subject per source; evenly-spaced planes always including apex and base). Plus: **per-subject identity floors** in `baseline_identity.json` (raw per-subject PSNR is NOT comparable across this cohort — the ceiling moves with `D`/`dz`/FOV — so normalising by each subject's own floor is what makes any later slicing honest, and it surfaces an F1-class bug at epoch 0); a **non-finite guard before logging** (`_step` logged, and only *then* did `_run_steps_on_batch_chunks` check `math.isfinite` — so a NaN batch's metrics were already in the AverageMeter, which then reads NaN for the **whole epoch**, and already in wandb; val had **no check at all**, and NaN is a value not an exception so every `try/except` passed it through — the guard suppresses **logging only**, train control flow untouched); **per-subject `try/continue`** in the identity-baseline loop (one `try` wrapped all N, and docs/59 F19 makes `get_data` raise for `D>20` subjects, which exist in the pool); `_save_val_volumes` affine `np.eye(4)` → `diag(dz,1.4,1.4,1)`. New metrics: `val/strata/source/*` (all 5 groups n≥15; the pooled mean hides a one-source collapse — 15 of 133 subjects moving 5 dB shifts it only 0.6 dB), `val/strata/pitch/{coarse_ge10mm,fine_lt10mm}` (the native-z tripwire, n=124/9), `{train,val}/resp/frac_slots_{offslab,dimmed}` (docs/59 F16 is accepted-not-fixed: the one-sided shift damages a **fraction** `d/((D−1)·dz)` that is far worse for short stacks, i.e. exactly the fine-pitch subjects native-z added — and it is unrecoverable post-hoc since the corruption is GPU-side and never persisted), EF **split by pathology** (GT-EF σ **16.2** diseased vs **6.2** healthy ⇒ a pooled slope is attenuated by range restriction and drifts with the val health mix), and `val/strata/*` re-pointed at the **heart-seg ROI** per the user's priority (`psnr_heartseg`, `mae_heartseg`, `recov_frac_heart`, plus the docs/38 **veto** `hole_frac_heart` — stratifying the electors without the veto lets a per-source coverage regression pass the ship gate; `psnr_bbox` demoted to continuity only, and `n` emitted **per metric** since the heart metrics are conditional and a shared `n` was last-write-wins). A `cohort/z_norm_max/*` summary was built then **removed** as exactly reproducible from the per-subject floors written seconds earlier. **Deliberately NOT added:** per-epoch vendor/centre/scanner/field-strength/pathology-detail/age-sex panels — val subgroups are n=1–19 and several taxonomies are not comparable across sources; all are one `groupby` away in the CSV, and vendor claims belong on **test** (Canon: **0 train subjects**, 10 val, 40 test). Confounding **measured** (NMI on val): source×pitch **0.476**, source×pathology **0.470**, source×vendor **0.412** ⇒ the cohort-wide 12-vs-10 mm split is a *source* comparison wearing a pitch label; the honest within-source contrast is CMRx2025 alone (24 @ 12 mm vs 13 @ 10 mm). Argues **"log raw, slice later"** over pre-aggregated subgroup means: with B=1 the per-subject value is already computed and the AverageMeter just discards it, `manifest.csv` has 23 covariates so the interesting cut is not knowable in advance, and per-cell `n` is tiny (val has 4 subjects @5 mm, 1 @7 mm, **0 @8 mm**). Also records 5 failure modes **dismissed by reading the code** (sampler imbalance, `D`-dependent loss/gradient scaling, aug at extreme pitch, torch.compile churn, splat coverage vs `D`) and one **latent issue left open** (`_compute_identity_baseline` passes `img_per_seq=20`, and F19 makes `get_data` raise for the `D=19/20/21` subjects currently all in test). A 2-agent debate then found and fixed 6 more real defects: the resp off-slab family was silently behind `if not self.wandb_writer: return` (breaking the offline promise for the ONE family that is unrecoverable post-hoc), `val/strata/.../n` collided across metrics, `metrics.jsonl` had one `step` field for **two step spaces** (val scalars use `steps['val']`, val panels use the train step ⇒ added `epoch` + wall-clock `t`; **join on epoch, not step**), `cardiac_phase.csv` (which *defines* the 266-pair val protocol) was unhashed, `split_md5` doesn't move when the VOXELS change (added `data_cache_signature` — this repo flipped 893 subjects' arrays in a week), and `slurm_node` was null (added hostname/GPU/CUDA). Added `tools/load_run.py::compare_runs` for **paired per-subject** cross-run deltas plus a commensurability check that refuses to compare runs on different data or protocol. ⚠️ Two of that debate's headline claims were **false** and are recorded as such — 'no CSV was written' (it inspected mid-run) and 'heart ROI valid for only 49/133 val subjects' (it omitted the `/sax` path component; real answer is **266/266 rows carry heartseg, all 5 sources, zero stale warnings**). **Open gap:** no per-subject identity floor for the heart ROI — structurally blocked twice (the loss gates heartseg off the identity pass, and the identity batch never carries `heart_roi_canonical`), so per-subject heartseg is currently only a within-subject trend; ~15 lines, deliberately not done since it touches `loss.py`. Verified: `pytest` **307** green, two real `--config default` runs (0 tracebacks, 266 CSV rows × 36 cols, 168 scalars all carrying epoch+t); EF-by-pathology is covered by unit tests rather than end-to-end because nnU-Net failed `rc=1` on all 5 retries in this environment (pre-existing flakiness, `retries=5` predates this change). |

| [`61_training_code_simplification_and_config_flattening.md`](61_training_code_simplification_and_config_flattening.md) | **Deletion + simplification pass over `training/`, done immediately before the pooled-1337 long run** (2026-08-01, DONE + verified). **~310 lines of inherited VGGT scaffolding removed** — gradient accumulation (dead **and broken**: B is pinned to 1, so chunking computes `start=end=0` ⇒ empty tensors), the DDP helpers `284992c` missed, dead batch keys (`rotations` was literally `np.zeros(3)`/slot), dead config keys, and a 68→16-line `copy_data_to_device` whose namedtuple/dataclass/Protocol branches never ran on a plain dict. **The 3-layer config chain `default → mri_finetune → mri_volume` was flattened to one runnable `default.yaml` + `exp_bspline.yaml`**: `mri_finetune` was a HALF config (no `loss.volume` ⇒ `MultitaskLoss(volume=None)` ⇒ `objective=0` ⇒ a silent zero-loss run) and the base layer **lied about the architecture** (advertised `frozen_module_names: ["*aggregator*"] # example`, `enable_point: False`, `compile_attention_blocks: False`, a placeholder `/YOUR/PATH/TO/CKPT` — all overridden). Verified by diffing Hydra's fully-resolved config before/after: **byte-identical**, all 3 configs; 36 references across 29 files re-pointed (incl. 4 in `evaluation/`). **Four real bugs surfaced, none caught by any test:** (1) `wandb_id`/`wandb_url` in `run_meta.jsonl` had **always been `null`** — `WandbLogger` kept `wandb.init()`'s return in a local while `trainer.py` read `self.run`; (2) `reslice_volume_vec(spacing=SPACING_MM)` defaulted to a **pre-native-z 12 mm** pitch and `baselines/export_resp_stack.py` relied on it ⇒ every non-12 mm subject silently breathed at the wrong scale (`spacing` now **required**); (3) 🔴 **training sampled subjects WITH REPLACEMENT** — `inside_random: True` discarded the sampler's permutation, so **~37% of subjects went unseen every epoch** (a different 37% each time) while the config claimed "935 = one exact pass per epoch"; removed, now the ML default (**verified 935 draws / 935 unique**, reshuffled per epoch); (4) `baselines/eval_all_baselines.py` was already dead (reads the `world_points` key deleted with supervised-DVF) *and* its elastix-vs-carmen arm compared two identical batches ⇒ archived. **The pass also caused, then fixed, its own regression** — the most instructive part: deferring the dataset's 64 MB/sample `images` build (which `gpu_augment_batch` overwrites 100% of the time under respiratory) broke `_compute_identity_baseline` + the filmstrip, which call `get_data()` **directly** and so bypass the "missing key ⇒ extract" contract; every subject raised `KeyError` into a per-subject `try/except` and the run produced **no `baseline_identity.json` at all**. `pytest` stayed green; only an end-to-end run exposed it. Fixed by making the shared helper self-sufficient **and** scoping deferral to train-only, after finding **12 scripts** (incl. `inference/run_cmrxrecon.py`) that read `data.val`'s images directly. **Two deliberate behaviour changes:** `default.yaml` now uses **L2 diffusion** (`tv_weight=0, diffusion_weight=1000`) not L1 TV — verified from the runs' own wandb configs that the entire docs/46 100-epoch series ran that arm, so the shipped config is now the one the evidence was collected on (`exp_diffusion.yaml` became a no-op ⇒ deleted; `exp_bspline` pinned back to L1 TV, else it would have silently changed both head *and* regularizer); and the **`recov_frac_heart` `[-0.5,1.5]` clamp removed** (it censored **98.9%** of rows to −0.5; confirmed on real data where all 8 val subjects sit at −1.4…−7.8). **`loss.py`'s five duplicated masked-metric blocks merged into two helpers, split by train-path (branchless `torch.where` — a Python `if` costs **4 measured graph breaks**) vs val-only (boolean-indexing loops)** — behind a **fault-injection-verified** golden harness (`tests/test_loss_masked_metrics_golden.py`, 10 tests; 3 injected bugs each caught, incl. a clamp fault that **initially slipped through** because the synthetic MSE never engages the floor, fixed by a feed-`V_canon`-back-as-`V_gt` case). Refactor proven behaviour-preserving by comparing **75 metric values at full float64 with `==`: 75/75 bit-identical** — ⚠️ **except `recov_frac_*`, which the synthetic harness structurally cannot reach** (`span > 1e-6` rejects every sample). Also: flip aug → **aggressive-tier-only** (moderate is the shipped docs/46 C2 arm and had no flip), EF/CorSeg → **every val epoch** (measured ~1.75 min on a ~4.2 min val loop), `self.target_size` made real (must stay a multiple of 14), viz batch-building deduped (the docs/59 F14 shape), `CorSeg/` gitignored, sbatch accounts → `jjparkcv0`, CLAUDE.md's stale `max_img_per_gpu`/`S=20` claims corrected (**S == D**, 5–21, under `one_frame_per_slice`). **Deliberately KEPT with reasons** (§8, read before "cleaning up"): the two masking styles, `mri_mode` (58 call sites; the config key survives via `exp_name` anyway), `per_slot`'s `RespiratoryConfig` field (4 `evaluation/` adapters + ~15 tools set it), the nnU-Net EF fallback, `freeze.py`'s `recursive`, val-side non-deferral, `img_nums` (**not dead** — it is the docs/59 F19 cap), and every native-z guard clause. Verified: `pytest` **335**, 6 clean end-to-end smoke runs, worker-seed preservation proven over 8×3×200×3 combinations (the old formula's `world_size` term contributed **+1**, so a naive DDP-deletion would have silently reseeded every worker). 🔴 **NOT verified: multi-epoch + SLURM requeue, still never exercised end-to-end** — now the largest untested surface, since this pass rewrote the train step, sampler, collate path, dataset output contract and config layout. Do it before the long run. **(→ closed by docs/62, which also independently confirms the bit-identity claim by running the pre-refactor code.)** |
| [`62_refactor_verification_audit.md`](62_refactor_verification_audit.md) | **Independent adversarial re-audit of the whole docs/60+61 range (`e1c38ac..527093c`)** (2026-08-01, DONE — refactor CLEARED for the long run). 13 agents (9 whole-target reviewers + 4 refutation verifiers) plus runtime checks on an L40S. **The bit-identity claim is confirmed by EXECUTION, not by reading**: a pre-refactor **git worktree at `e1c38ac`** was run against the same data and the same node-local cache → **72/72 batch tensors bit-identical** (4 real subjects, native-z `D`=10/11/12, aug + respiratory on, deferral active; only the deleted dead keys `ids`/`rotations` differ), and every loss/metric value equal to within **3.6e-7 relative — *below* the 1.8e-6 same-code CUDA rerun noise floor** (the splat's scatter-add makes the same code differ from itself; **measure that floor before interpreting any delta**), the sole exception being `recov_frac_heart` = the deliberate clamp removal. `vggt/` is untouched ⇒ identical batch + identical model + identical loss ⇒ **the whole train step is bit-identical by composition**; also confirmed byte-identical `preprocess/freeze/gradient_clip/logging`, an exact **344-frozen/930-trainable** replay against a real pre-refactor run's recorded lists, worker seeds `43-46` / `12388` matching `worker_id+seed+1+epoch*12345` at runtime, and `pytest` 256→**335**. **docs/61 §10.1's gap is CLOSED**: 4 epochs across 2 launches, `resumed_from_epoch=2`, 514 scalars, 24 per-subject rows, `load_run.py` round-trips, 0 tracebacks (residual: the resume was at an *epoch boundary*, so the duplicate-`(name,step)` dedupe path is still only simulated; a real `scontrol requeue` would requeue the interactive session's own job). **Four real defects, ALL diagnostics/robustness, none touching training numerics:** (1) 🔴 `Train_Visuals_Augmentation` is **permanently dead** — `defer_input_images` made the previously-vacuous `"images" in batch` guard at `trainer.py:976` bite; runtime-confirmed (every other panel logged, this one absent from the wandb transaction log); (2) 🔴 **ED val panels are silently overwritten by ES** — the gate moved from `val_idx` to `subj_idx` so both sweep halves now match, but the key and the frozen `steps["train"]` were left alone ⇒ all **15 keys written twice at one step**, wandb keeps the last (measured against the real 133-subject sweep + an offline wandb transaction log showing **1 history record, ES only**); (3) 🔴 **`frac_slots_dimmed` is 100% false positives** — the reslicer zero-pads with `align_corners=True`, so a landing in `(0,1)` interpolates between two REAL planes; measured, the `dimmed` band fires exactly where retention is **1.0000** and never on attenuated slots (**0 overlap** across 6 `(D,dz)` pairs, at a 7–35% reported rate), and ⚠️ **the unit test encodes the same wrong band**, so it would BLOCK a fix; `frac_slots_offslab` is numerically right but means "any attenuation", not "blanked"; (4) 🟠 **`_widen` commits the widened column list BEFORE the rewrite succeeds** (`run_log.py:188`) — fault-injected ENOSPC, and a reader then silently saw **2 of 4 rows**. Lower severity: **`seed_value` no longer reaches the sampler** (measured — `seed_value=42` and `=7` give the *identical* permutation; pre-existing wiring made load-bearing by the `inside_random` removal, so a second seed would reuse run 1's order), **4 `tools/` entry points crash at Hydra compose** on deleted config names (incl. all 10 cases of `verify_env_migration.sh`, the script prescribed for dependency bumps), `_ef_stats`' degenerate-spread guard misses the exact "54/55/56" case its own docstring cites (measured `slope: 10.0`), and `target_size` is half-threaded so `img_size != 518` went from *silently ignored* to *hard crash*. **⚠️ FIVE deliberate behaviour changes, not the two usually cited** — beyond `inside_random` and the clamp: flip aug → aggressive-only (the shipped `moderate` arm's data stream changed), EF → CorSeg every val epoch, and least obviously **`sbatch/train_mri_volume_reference.sh` silently switched regularizer arms** (`mri_volume` `tv=0.1` → `default` `diffusion=1000`) when its `CONFIG=` was repointed, with its header still saying otherwise — `exp_bspline` is now the only surviving `tv=0.1` arm. **Two claims REFUTED** (recorded so they are not re-litigated): mid-epoch requeue starvation (the old worker RNG was *also* a pure function of epoch, so restarts replayed the same prefix before too; and the hazard needs walltime < 1 epoch when shipped is 14 days vs ~34 min), and `export_resp_stack.py`'s `range(12)` (real, but byte-identical at `e1c38ac`, from the native-z migration, and already unreachable behind a deprecated split file). Also corrects **docs/61 §6.2** — the synthetic harness *can* reach `recov_frac_*` — and lists 7 stale comments/strings. Lessons: bit-identity is a claim about execution; random-input probes are weak evidence for last-ULP questions (a CPU-vs-CUDA probe on random data wrongly refuted a correct hypothesis that real data confirmed instantly); a guard that is vacuous when written becomes a silent bug when its precondition changes; a test can encode the bug. **§11 (2026-08-01) records the RESOLUTION:** all 4 defects + all 4 lower-severity items + all 7 stale-text items fixed, each proven against a check first shown to FIRE on the pre-fix code (`dimmed` matches the real reslicer at 17/17 landings; `_widen` fault-injection 2→4 readable rows; seeds 42-vs-7 now differ with 42 bit-identical, and a re-run of the §2.1 comparison confirms **72/72 batch tensors still bit-identical** at seed 42, so the shipped config never moved) — **except** the ED/ES collision, **accepted not fixed** because `media_val_ED_ES` already carries the ED+ES pair (the proposed index-arithmetic key fix could mislabel a panel, which is worse than a known overwrite). The five §4 behaviour changes are **confirmed intended**, incl. the reference sbatch staying on L2 diffusion. Also corrects an error in docs/62 itself: only `cmrxrecon.py` passes `n_planes`, not all four adapters — harmless because the other three pass no `group_ids` either, so they never reach the fallback. **And an error in the RESOLUTION itself (§11.7): the §5.1 aug-panel fix shipped broken on its first pass** — it omitted the `(B,S,518,518,3)/[0,255] → (B,S,3,518,518)/[0,1]` conversion `gpu_aug` does at all three of its own assignment sites, so `_gray`'s `.clamp(0,1).mean(dim=1)` saturated the values and averaged over H instead of the channel axis, rendering the "original" row as a **518×3 near-white sliver** — and the "panel appears in the transaction log" check **passed anyway**, because it verified *presence*, not *content*. Corrected, then re-verified on the rendered PNG's pixels (original row std **0.436** = real anatomy; a saturated row reads std ≈ 0). ⇒ **for a diagnostic fix, "it now emits something" is not a proof.** Finally, a post-fix re-sweep found **one launch-blocker the original audit missed** (§11.8): 8 `sbatch/*.sh` still passed `max_img_per_gpu=12`, deleted in docs/59 F9, and Hydra **refuses to override a key that does not exist** (`+` required to add one) ⇒ `ConfigCompositionException` at `compose()` — fast and loud, before the model loads, and **`train_pooled1337_nativez.sh` was never affected**. Pre-existing, cost only the docs/46 ablations' reproducibility. Fixed; **55/55 override sets across every `sbatch/*.sh` now compose.** ⇒ a config-rename sweep must grep for dead override **keys**, not just dead config **names**. |
| [`63_synthetic_data_engine_design.md`](63_synthetic_data_engine_design.md) | **Design doc for a SynthSeg/SynthMorph-style synthetic data engine — NOT implemented, staged plan only** (2026-08-04). Establishes that the engine's *preconditions* hold here: the model is a **pure geometric transport operator** (point head emits Δ only; `splat_predictions` moves input intensities verbatim), and the photometric `loss.py:195` L1 is **geometric in disguise** because `V_gt` (`gpu_aug.py:335`) and the input slices (`:392`) are both re-extracted from the same `phases_aug` tensor — so any appearance map `f` changes both sides identically and the optimum in Δ is unchanged. ⇒ **synthesis must be inserted on `phases` at `gpu_aug.py:317`, before the clamp (`:328`) and before `V_gt` is derived (`:335`); `V_gt` becomes synthetic too, and that is what keeps the loss well-posed.** The design that must be REFUSED is real `V_gt` + synthetic inputs (systematic intensity bias, no channel in the head to absorb it ⇒ gradient descent answers it by distorting geometry — the repo already hit the miniature version, hence the `:324-328` clamp). **Headline finding: the highest-value item is NOT contrast randomization.** `gpu_aug.py:373-386` already samples the exact GT respiratory displacement into `batch["resp_disp_mm"]` (B,S,3 mm) and marks it *"Inert for model/loss"* — **verified: `grep` finds it in `tools/` only**, zero hits in `training/loss.py` or `training/trainer.py`. So free-supervision **channel (d) (the transform you sampled) is already computed and discarded** every step, while supervision flows instead through an L1 whose through-plane gradient docs/19 measured **2× weaker** than in-plane (0.0067 vs 0.0143 /mm). **detect-vs-ignore rule** governs what to randomize: **respiratory = pure nuisance ⇒ randomize past realism** (docs/08 already measures the sim 3–50× too large, no harm); **cardiac = the signal the model must reproduce at `t_target`** — but the naive "don't randomize it, you'd destroy the prior" objection is **overstated and was reversed**: hearts do not contract alike (docs/39 EF 63±10 preserved vs **21±9 DCM**), so a population prior is weak and randomizing *forces content-based inference*, exactly SynthMorph's stance. The real bound is a **disanalogy with SynthMorph**: each plane is seen once and the reference anchor sits at a *different* `z`, so there is **no fixed image per plane** ⇒ correspondence is only **partially identifiable** ⇒ in-plane contraction is recoverable (the win, esp. at pathological amplitudes) while through-plane shrinks toward zero — which **is** the already-measured docs/07 slope **0.42** / corr **0.87** (docs/33: 0.35 / 0.52), i.e. correct `r`-blind Bayesian behaviour, not a defect. ⇒ simulate cardiac motion but keep it **cardiac-shaped** (torsion + longitudinal shortening + radial thickening) with amplitudes pushed past physiological. **Synthetic ANATOMY is rejected** on docs/20's population template = **14.4 dB, below the 16.8 identity floor** (hearts unaligned in the canonical grid; anatomix's .74/.68/.57 label-source ablation says correspondence tasks need *matched* priors and a mismatched real prior is worse than noise) — 1337 pooled real subjects already supply it; real **CT-derived** masks are a separate, defensible option but inherit invented beating. **Expectation management, stated up front: this buys calibration + OOD robustness, NOT in-distribution PSNR** — docs/19 puts the model **20.6 vs a 21.2 direct-optimization ceiling** (oracle 35.0), so perfect motion estimation is worth **≤0.4 dB** and the wall is architectural; and DVF supervision **does not fix the 0.42 slope** (regression onto a GT the inputs don't determine converges to the conditional median — that *is* the shrinkage; needs an `r` channel, docs/04 §7, headroom capped ~1.9 dB by docs/33). What it *does* buy: (1) a **non-gameable mm metric** where docs/07 says outright *"there is no motion ground truth"*; (2) the **10.3 mm spurious MIITT relocation** on clean OOD stacks (vs 0.18 mm in-dist, docs/46) that photometric L1 structurally cannot see under a 16.0 dB cohort floor; (3) a gradient path bypassing the coverage division + uniform supervision on the 3–5% dynamic voxels (docs/13). **Staged S0→S4, each independently falsifiable:** S0 (~2 h, no training) wire `resp_disp_mm` → `metric_dvf_epe_mm`, verify it reproduces docs/07's 0.42 ±0.03 else the readout is broken; S1 `dvf_weight>0` on respiratory only — **kill if MIITT clean Δz doesn't move off 10.3 mm**; S2 real-anatomy + sampled cardiac branch at `prob=0.5` *alongside* the real-cine branch (never replacing it — docs/20 already observed the model hallucinating the subject-specific component at the population mean); S3 acquisition physics (bSSFP transient, R=8 aliasing, intra-frame blur) as **input-only** corruption in the respiratory slot, targeting docs/13's **−3.03 dB** single-shot gap, the largest measured drop in the repo — ⚠️ **must be applied to `slices_canon` BEFORE the 256→518 interpolate**, since aliasing lives in 256² k-space; S4 per-label GMM last, because the whole existing aug stack measures only **+0.41 dB pooled OOD** (docs/46 C2) and a GMM is a widening of that same axis, while every documented OOD failure is orientation/resolution/geometry-driven (MIITT ~180° off; docs/08's OOD-clean paradox found the *most* appearance-divergent cohort looks *best*). **Labels are the hidden cost:** `heart_seg` (docs/39, 421+150 units) covers **≈1.6% of the canonical FOV** (arithmetic from docs/39 stats, unverified on volumes) with atria/valve-plane/great-vessels entirely unlabeled, so SynthSeg's **k-means filler-label trick is load-bearing, not garnish**, and `heart_seg_canonical.nii.gz` **does not exist yet** (only `heart_roi_canonical`, `mri_dataset.py:604-620`). Separately: the **masked volume loss is already commented out at `loss.py:194`** and needs no engine at all — cheapest item in the doc. **Risks logged:** frozen 304M DINOv2 cannot adapt to wide draws (cheap zero-training CKA probe proposed; docs/46 C4 says nothing about the interaction); learning-the-simulator; and **a supervised-DVF path existed and was deleted** (`29da7b3`, −563 lines) — but that GT was elastix/carmen *registration output* and docs/48 shows `compute_point_loss` was **never reached** (weights all 0), so it died by architecture pivot, not measurement — while docs/48 equally shows how a `weight: 0.0` slot rots into dead code, so S0/S1's metric-only mode must be defended by a **test**, not a config default. Also flags that through-plane/obliquity randomization is currently **zero** (`gpu_aug.py:108-109`) despite measured `|S|` median **0.402**, and that everything here sits behind two bigger levers: the **input contract** (docs/21's 18.2→13.55) and the **splat = 75% of the blur** (docs/10). |
| [`64_pooled1337_dying_relu_collapse.md`](64_pooled1337_dying_relu_collapse.md) | **Why the pooled1337 aug/noaug long runs (SLURM 55996915/16) died at epoch ~17 of 300 — PROVEN, not inferred** (2026-08-04). `point_head.scratch.output_conv2[1]`, the `ReLU(inplace=True)` between the DPT head's last two convs, is **100% dead** in both checkpoints: every one of ~34M pre-activations is ≤0 (aug max **−1.45e-05**, noaug **−0.138**), in **all 32 channels**, for arbitrary inputs. Each link of the chain measured: dead ReLU ⇒ output exactly 0 ⇒ final 1×1 conv emits **only its bias** ⇒ DVF **bit-exactly spatially constant** (per-channel spatial std `[0,0,0]`, 100% of neighbour diffs exactly 0) and **input-independent** (two random batches differ by **2.06e-12** relative) ⇒ `loss_diffusion = ‖∇u‖²` **exactly 0** (1.02e-18, reproducing the log) ⇒ ReLU derivative 0 **severs the upstream gradient**: norms fall 7 orders across it (`output_conv2` 4.08e-2 → `output_conv1` **2.42e-9** → `grad_aggregator` **1.56e-10**) vs a base-VGGT-1B control at **1.81** on the same batch. The `grad_point ≈ 0.5` that looked healthy in the logs is **only the final conv's bias gradient** — the sole trainable param downstream of the ReLU, which is why the standard gradient panel never flagged it. The aggregator itself is undamaged (tokens still spatially varying); it is merely starved, then **actively degraded** — AdamW `weight_decay=0.05` shrank every trainable LayerScale gamma **2.9–3.6×** over the 64k zero-gradient steps, matching `(1−lr·wd)^64400≈0.42`. Nothing exploded (max|w| 4.0, zero non-finite). Corroboration that this is a hard stop, not a plateau: the two arms became **numerically indistinguishable** from epoch 17 (ep30 19.1031 vs 19.1005, and per-subject), and train loss ≈ val loss and *worse* than its own epoch-11 best ⇒ not overfitting. **Trigger: high confidence, not certainty.** `3e-4` peak LR is a **6× override** of `default.yaml`'s `5e-5` applied to ~636M **pretrained** params at **batch size 1**; both arms survived the full 15-epoch warmup and died 1–2 epochs after hitting peak, within one epoch of each other. ⚠️ **The noaug arm died with `grad_point ≈ 2.3, no spike at all**, which REFUTES the "single unlucky batch" reading the aug arm alone suggests (aug survived 39 steps at `grad_point>20` in epochs 0–1) — it is **progressive drift** whose rate LR sets. **Not separable from native-z** (docs/58): the only native-z-at-5e-5 control is healthy (0/32 dead) but reaches only **epoch 3**, and drift is already visible there (mean pre-activation 1.550 base → 0.299). **Applied:** `PEAK_LR`→`5e-5` in both sbatch scripts (verified all THREE LR knobs move via Hydra) and `RESUME_FROM` cleared — resuming was a trap, both saved checkpoints are post-collapse and docs/37's full-resume would have restarted the dead model at `prev_epoch=86`. **Recommended, not done:** a tripwire (`grad_aggregator<1e-6` for N steps, or `loss_diffusion==0`) — this was silent for 70 epochs × 2 GPUs; **clipping is not the answer**, `max_norm=1.0` was active throughout. **LR QUESTION SETTLED BY EXPERIMENT** (`tools/exp_relu_death_vs_lr.py`, 4 arms x 3000 steps, constant LR on the real pipeline from real base weights): it is a **bifurcation, not 1/LR scaling**. At **5e-5 / 1e-4** the system reaches a **stable equilibrium** (dead-channel fraction plateaus 0.20/0.35, `pre_max` stops falling and RECOVERS 6.16->12.00, `grad_aggregator` healthy 1.5e-2/2.1e-2); at **2e-4 / 3e-4** it collapses (`pre_max` monotonic 1.93->0.91, `grad_aggregator` 1.3e-5/3.7e-5, ~1000x lower). Threshold is between **1e-4 and 2e-4**; production ran at 3e-4. => **5e-5 does not merely delay the collapse, it does not collapse**, and **native-z is exonerated as a sufficient cause** (present in the stable arms too). Figure `figs/relu_death_vs_lr.png`. Also shipped: **gradient-collapse alarm** emailing via `train_utils/notify.py` (`optim.grad_collapse_alarm`, threshold 1e-6/patience 200, fault-injection tested both directions) and **weights-only best-by-val checkpointing** (`checkpoint.save_best`, `metric_psnr_3d_heartseg`, ~3.8GB). Repro: `tools/probe_aggft_collapse.py` (base-model control included, so the zero readings are proven to fire on a healthy model). |
| [`65_claude_md_offloaded_details.md`](65_claude_md_offloaded_details.md) | **Verbatim archive of everything trimmed out of CLAUDE.md on 2026-08-07** (~250 → ~140 lines): refactor/config/flat-EF history, the slice-order standardisation block, full augmentation + respiratory parameter detail, CKPT_ONLY gotcha mechanics, PYTHONPATH evidence, logging panel/metric minutiae. Nothing deleted — grouped by original CLAUDE.md section, each item noting its owning numbered doc. Look here first if a trimmed CLAUDE.md line seems to be missing context. |
| [`66_fresh_bootstrap_4wok_regression_audit_and_experiment_plan.md`](66_fresh_bootstrap_4wok_regression_audit_and_experiment_plan.md) | **Fresh-bootstrap/4wok regression audit + controlled experiment plan** (2026-08-08). Reconstructs the actual W&B 4wok recipe versus the later 8 mm Git-worktree replay; records the warm/fresh EF gap and the invalid dying-ReLU `cond_ratio` diagnosis; reports the all-294-subject direct12-vs-fixed12 voxel audit (**293/294 phases exact, 294/294 masks exact**) and the real-batch numerical audit (**fused AdamW negligible; compile forward-close but first-update cosine 0.48 vs eager**); classifies every known historical deviation as cleared/live/conditional; and preregisters a ten-run, step-aligned campaign led by faithful old direct12, compile-off, old index-z, and respiratory controls, all scored with one CorSeg EF/phase-transfer protocol before final pooled-native-z confirmation. |
| [`67_fcsvr_deep_dive_and_baseline_integration.md`](67_fcsvr_deep_dive_and_baseline_integration.md) | **FC-SVR (CVPR 2024) codebase deep-dive, two-stage pipeline proof, and baseline integration strategy** (2026-08-09). Exhaustive paper, code, and 3-agent debate analysis of FC-SVR (`seannz/svr`, arXiv:2312.03102). Proves two-stage separation (Stage 1 `Flow_SNet` motion SVD loss vs Stage 2 3D U-Net L2 loss), ping-pong `[xs, xw]` feature concatenation in `flow_SNet4.py:50`, step schedule equivalences (256k steps = 2,133 epochs on CMRx240, ~2.5 h walltime on 1 A40 GPU), single-stack physics, and reviewer-proof manuscript framing. |
| [`68_fcsvr_cardiac_stage1_protocol_and_doc67_corrections.md`](68_fcsvr_cardiac_stage1_protocol_and_doc67_corrections.md) | **Final FC-SVR cardiac Stage-1 protocol and corrections to doc 67** (2026-08-09). Records the immutable author-core boundary, CMR24 slab/respiratory/mask/training/evaluation contract, exact released vs literal-paper motion metrics, L40S forward/backward + resume + deterministic-validation evidence, complete adaptation limits, and corrections to doc 67's batch/epoch/runtime/holes/core-patch overclaims. |
| [`69_exp66_results_zhalfmm_respiratory_interaction.md`](69_exp66_results_zhalfmm_respiratory_interaction.md) | **exp66 campaign RESULTS — wave 1 is NOT a negative; `Z_HALF_MM` × respiratory is a real interaction** (2026-08-08, **REWRITTEN 2026-08-11 after its own first conclusion was falsified**). Arm **x1** (`Z_HALF_MM` 90→66 **AND** respiratory off) scores **`amp_ratio` 0.526** vs a 0.142 control and 0.740/0.747 anchors — the campaign's **first positive**, and a genuine **two-factor interaction**: neither knob works alone (a2 old-z-only **0.140**, a3 resp-off-only **0.152**), so a one-factor campaign was structurally blind to it. It was missed because x1's recorded score (**0.115**) came from a dump decoded through the **wrong tree** — x1 must decode via `vggt-arm-a2` at 66. Documents **two live footguns in `tools/e0_dump_phase_sweep.py`**: dumps record **no provenance**, and **`ef_val_sweep` defaults true and silently overrides `t_target_fixed`** (a "phase sweep" then decodes the same ED/ES pairs 12×, scoring `nan`) — with the verification recipe for both. **The data lead is DEMOTED** (x1 bootstrapped on v2 data ⇒ data is not *necessary*), though v1↔v2 is fully characterised: apex flip + odd-Z −1 roll (parity-gated) + the docs/54 **ESPIRiT B1− shading bug** (v1 peaks ~3.5–4× at the LV, decays ~11–13× to the periphery; v2 flat; v1 also hard-zeroed 0.3–3% of body voxels, one-directionally). Retains: **P0 (June's complete tree) fails at 0.119** (unverified provenance); **p0dino (stem unfrozen) refuted and harmful**; **`pearson_median` ≈ 0.98 in every arm** ⇒ amplitude broken, timing intact. Mechanism §7 **retracts** the "66 gives better depth resolution" story (the embedder has π/2π/4π *plus* a linear term) in favour of **z-vs-in-plane gradient balance** (`z_scale` 7.5 vs 5.5; breathing is a z task), with a native-z-portable test — needed because **66 cannot be used under native-z** (aliases and hard-raises). Also records the **heart-ROI L1 series** (w=0/0.10/0.5, jobs 56990551-3; heart is only 4.3–4.6% of the volume; scale is `1+w/frac` so w=0.5 ⇒ ~11×) and the **never-run 4wok premise check** that still gates every interpretation. |
| [`71_amp_transition_time_heartl1_verdict.md`](71_amp_transition_time_heartl1_verdict.md) | **Amplitude bootstrapping is a TRANSITION-TIME phenomenon; heart-L1 w=0.5 is the confirmed, shippable accelerator; CorSeg-Dice gives no acceleration** (2026-08-12). Every heart-series arm on v2 data transitions — including the unmodified control (~43.9k steps) — and onset tracks the loss's effective heart weighting monotonically: **w=0.5 → ~8.9k** (offline-confirmed amp_ratio **0.640** vs control 0.114, distributions non-overlapping, 28/29 subjects, BEST motion PSNR 22.30), v1-shading implicit ~2× → ~23–30k (user wandb), w=0.1 → ~32.7k, plain v2 → ~43.9k, x1 → ~51k. Reframes doc 69/70: the v1↔v2 flip is a large transition-time shift, not on/off (P0's 0.148@ckpt60 = pre-transition read); the scoring-time confound is CONFIRMED first-order. CorSeg-Dice flat at ~41k at both weights on a metric biased its way, worse motion PSNR — dominated by heart-L1; neutral-vs-harmful verdict deferred past the control's ~44k onset. Ship decision: **promote heart-L1 w=0.5 to pooled native-z**. Also records the verified scoring-recipe gotchas (old-tree has no `ef_val_sweep` key; v1 `data_root` must be ABSOLUTE; per-dir 58-file/one-label check). |
| [`72_input_output_resolution_experiments.md`](72_input_output_resolution_experiments.md) | **Resolution-flow audit + `b2ck5kfd` experiments** (2026-08-12/13). Actual trained-model inference shows 518²→native-256² rendering costs only **−0.118 dB bbox PSNR** over 10 clean val subjects, while direct rendering into 128 z planes collapses coverage **77.4%→12.1%** (bowed acquisition sheets, not z super-resolution). Records every positional mechanism (DINO learned absolute PE interpolation, VGGT 2-D RoPE, DPT UV sin/cos), the 224² mechanical path (16² patches, 261 tokens/slice, 224² DVF → native `(D,256,256)` splat), the zero-shot probe and corrected aggregate-provenance caveat, the exact four-line temporary training-tree diff, scripts/figures/runtime locations, timing caveats, and the home-quota incident/recovery. The 224 pilot is ongoing; final quality verdict pending matched-step motion/EF evaluation. |
| [`73_native_first_pipeline_port.md`](73_native_first_pipeline_port.md) | **Native-first pipeline port: `img_size` knob + always-native splat** (2026-08-13). The tracked pipeline now supports any multiple-of-14 model-input resolution (518 default, 224 validated by the docs/72 pilot) and ALWAYS splats the original native 256² slices: one native extraction (`batch["images_splat"]`, resp-corrupted), model input = a resample of it, loss resamples the predicted field back to 256². `gpu_augment_batch` is now mandatory batch finalization (requires phases/scanner_coords/timesteps/slice_indices; mutating `images` after it no longer reaches the render). Verified: 2× 3-reviewer prove-it, 347/347 tests, GPU probes showing render invariance across R. **Series break**: ~0.1 dB at 518 AND coverage/hole_frac see ~4.1× fewer points — never compare hole_frac across the port (also retroactively inflates the docs/72 pilot hole_frac gap). |
| [`74_wandb_visualization_and_intensity_controls.md`](74_wandb_visualization_and_intensity_controls.md) | **W&B visualization coverage + intensity controls** (2026-08-13). Records the nine source/vendor subjects, explicit ES-only ordinary panels plus combined ED/ES panels, separate all-subject 12-phase GIFs every 5 epochs, display-only gamma 0.7 coverage/exclusions, configurable phase-0 nonzero-FOV `intensity_percentiles` (default p0.5–p99.9) with cache invalidation, p2–p98 experiment status, runtime cost, full 266-target A40 validation, 352-test proof, and two deliberately ignored diagnostic-only edge cases. |
| [`75_vggt_omega_selective_intermediate_caching.md`](75_vggt_omega_selective_intermediate_caching.md) | **VGGT-Ω selective aggregator-output caching** (2026-08-13). Retains only DPT layers `[4,11,17,23]` (also covering B-spline's final-layer input) and replaces the other 20 returned tensors with `None`. A40 measurement: model F+B peak saves **2.10 GiB at S=10 / 4.40 GiB at S=21** (13–16%) for only **0.16–0.24%** steady-state runtime improvement; final GPU outputs and CPU gradients are bit-identical. Records consumer audit, zero attention-FLOP change, full-step scope caveat, and reproducible benchmark. |
| [`76_224_compile_scope_profiling.md`](76_224_compile_scope_profiling.md) | **Complete 224-resolution compile-scope benchmark + shipped decision** (2026-08-13). One merged toggle now compiles **24 frozen DINO + 48 VGGT blocks** individually: stabilized A40 step **961.8→716.1 ms**, allocated VRAM **26.24→23.01 GiB**, ~58 s cold compile, five graphs, zero breaks/recompiles across variable `S`. Rejects DPT/whole-aggregator/whole-model/full-loss/max-autotune scopes with measured reasons; records checkpoint-off ceiling and the crucial non-equivalence result (compiled coordinate drift up to `9.33e-5`, amplified by splat boundaries ⇒ fresh numerical series). |
| [`77_dinov3_backbone_port.md`](77_dinov3_backbone_port.md) | **Complete DINOv3 ViT-L/16 port + handoff** (2026-08-13). Config-selectable 256²/patch-16 DINOv3 with unchanged DINOv2 default; records every code/config/checkpoint decision, strict/z rationale, all four prove-it fixes, worktree provenance, and verification evidence. Transformers 5.3.0 is installed cleanly; gated access is authenticated; and the 3.76 GB physical hybrid seed is SHA-256 verified, strict-loaded, and real-seed A40 forward/backward tested. **Ready for a bounded training pilot; no training launched yet.** |
| [`78_miitt_gated_into_training_pool.md`](78_miitt_gated_into_training_pool.md) | **MIITT gated cine added to the training pool** (2026-08-16). The U-Michigan paired gated/real-time dataset (10 volunteers + 3 patients) becomes a training source: `tools/convert_miitt_to_12phase.py` picks 12 of 30 gated phases (ED-anchored, no interpolation) into `scratch/data/MIITT_sax/`, mirroring `ACDC_sax`/`MNMs_sax`, source tree read-only. Cohort 935/133/269 → **940/136/274** via `training/splits/pooled_miitt.txt` (= `pooled.txt` verbatim + 13 lines; hand-assembled so `build_pooled_split.py` doesn't reshuffle CMRx2025). Epoch cost +0.5%. Data was already LPS / real-spacing (dz=10 mm) / **apex-at-z0 13/13** — verified, no flips. **Three traps recorded: (1) all 13 subjects resolve to `subj_id="gated"` under the source layout; (2) `heart_weight>0` HARD-RAISES without `heart_roi_canonical` (rebuilt free from the shipped Task114 seg, no GPU); (3) `default.yaml`'s hardcoded `limit_val_batches=266` truncates the ED-then-ES sweep so it deletes exactly the new val subjects' ES volumes, killing their EF silently.** Verified by a 1-epoch MIITT-only smoke (`loss_heart` nonzero, `[ef] n=3 skipped 0`, `source:MIITT` strata) + 367/367 tests. Sbatch ready, **not submitted**. |
| [`79_eval_harness_nativez_rebuild.md`](79_eval_harness_nativez_rebuild.md) | **`evaluation/` + `inference/` rebuilt on the native-z contract** (2026-08-16). The harness had drifted behind training: 4 hard crashes plus a SILENT one — three hand-written copies of the retired z convention `z/(D-1)*2-1` (a 1.8x through-plane stretch on ACDC_patient006), and a 4th in the SCORER, where `assemble_and_gif` force-resampled every volume onto a hardcoded `(256,256,12)` grid *after* the model had reconstructed it correctly. Fixed by **deleting the second implementation**: `tools/convert_ocmr_to_12phase.py` → `OCMR_sax/` removed the last source without an `MRIDataset` entry (8 subjects, mixed slice order → **3 apex-first / 5 base-first**, flipped per-subject INSIDE the converter so `fix_slice_order.py`'s 893-subject provenance sidecar is never overwritten), so the harness is now `trainer.val_epoch` with breathing supplied by frozen pixels. **≈2,400 lines + 264 GB archived (moved, never deleted); ≈700 written.** Protocol now comes from the ckpt's own `run_meta.jsonl`, not the live `default.yaml` (which resolves to `img_size 518`/`moderate` while the arm trained at **224/aggressive**); `--regime`/`--continuous-z`/`--refiner` deleted as run properties or no-ops. **Verified vs the trainer's own `val_per_subject.csv`: 29.62 vs 29.49 dB (heart ROI), 30.78 vs 30.39 (bbox)** — fault-injected (wrong `z_scale` → 21.9). Adding val subjects is incremental: both draws key on the subject NAME, proven byte-identical under head-of-`[val]` insertion. **Two claims MEASURED AND CORRECTED: the old builders were NOT mis-breathing** (grouped and per-slot paths are bit-identical when `S == D` and `per_slot=False` — the match was real, just resting on two coincidences), **and the ~9 dB harness/trainer gap is a PSNR normalization difference**, exactly `20*log10(gt[roi].max()) = -9.04 dB`, now reconcilable via the new `psnr_unit_peak`. |
| [`70_corseg_dice_arm_heartl1_aug_fix_and_doc69_reassessment.md`](70_corseg_dice_arm_heartl1_aug_fix_and_doc69_reassessment.md) | **CorSeg-Dice arm + heart-L1 ROI aug fix + doc-69 causal reassessment** (2026-08-11). **Doc 69's interaction is DEMOTED; the v1→v2 DATA change is promoted** — user-verified from wandb that P0 (old code, v2) does not contract while p0v1 (identical code, v1) does; old code is index-z (66-equivalent, z-grad 5.5, resp ON) so the 2×2 *predicts 4wok should have failed* — it didn't. Debate synthesis: amplitude is a marginal second-order signal with interchangeable easing levers {v1 shading ≈ implicit heart-weighted loss (~1.2–2× post-norm, acting via the splat's contrast-weighted POSITIONAL gradient — L1 is sign-only per voxel), 8mm smoothing (replay verified on v2 data), resp-off@66}; plus a **scoring-time confound** (x1 flat for 50k then transitioned at ~51–54k; P0 scored at 25–35k). **Bug fixed:** gpu_aug never warped `heart_roi_canonical` → heart-L1 arms trained on a ±180°-misaligned ROI — fixed (fault-injected IoU 0.996 vs 0.000), w010/w050 restarted (57022432/3). **CorSeg-Dice arms** (soft Dice through a frozen CorSeg/MedNeXt-L on every V_canon slice vs `heart_seg_canonical[..., t_target]`; label remap verified IoU 0.90/0.71/0.89; Dice-on-GT 0.918 through the differentiable path): first launch **DIED at step 33 — §3b post-mortem**: finite loss + non-finite backward + no bf16 grad guard ⇒ one optimizer.step() wrote NaN into 930 weight tensors (three plausible theories — dead ReLU, gradient desert, bf16 z-score overflow — each falsified by measurement first). Fix stack: trainer skip-step guard on non-finite grads (load-bearing), fp32 island + sd floor, per-voxel grad clamp (insurance), chunked+checkpointed MedNeXt (6.85 GB @ D=21, 386 ms). Verified by 100-step smokes at BOTH weights; relaunched as **57037405 (w=0.002) / 57037406 (w=0.1)**. Gotchas: `composed_dataset.py` whitelists sample keys; amp_ratio is CorSeg-derived ⇒ **re-score decisive ckpts with nnU-Net**. |
